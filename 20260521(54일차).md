# 📅 2026-05-21 - C++, 컴퓨터비전, 리눅스

# 📅 2026-05-21 과제 제출본 (C++ & OpenCV 컴퓨터 비전)

## 1. C++: Fraction(분수) 클래스 구현 및 연산 과제

### 📌 과제 요구사항
1. **정수형 분자(`numerator`)**와 **분모(`denominator`)**를 관리하는 `Fraction` 클래스를 설계합니다.
2. 캡슐화 원칙에 따라 데이터 멤버는 `private`으로 지정하고, 외부 접근을 위한 **Getter/Setter** 및 생성자를 구현합니다.
3. **기약분수 변환 메서드(`reduce`)**와 **두 분수의 합을 구하는 연산 메서드**를 포함합니다.

### 💻 C++ 소스코드
```cpp
#include <iostream>

class Fraction {
private:
    int numerator;   // 분자
    int denominator; // 분모

    // 최대공약수(GCD)를 구하는 유클리드 호제법 함수 (내부 유틸리티)
    int getGCD(int a, int b) {
        while (b != 0) {
            int temp = a % b;
            a = b;
            b = temp;
        }
        return (a < 0) ? -a : a; // 절대값 반환
    }

public:
    // 생성자 (분모가 0이 되는 것을 기본 방어)
    Fraction(int num = 0, int den = 1) : numerator(num), denominator(den) {
        if (den == 0) {
            std::cerr << "오류: 분모는 0이 될 수 없습니다. 1로 기본 설정합니다." << std::endl;
            denominator = 1;
        }
        reduce(); // 생성 시 자동 기약분수화
    }

    // Getter / Setter
    int getNumerator() const { return numerator; }
    int getDenominator() const { return denominator; }

    void setFraction(int num, int den) {
        if (den == 0) {
            std::cerr << "오류: 분모는 0이 될 수 없습니다." << std::endl;
            return;
        }
        numerator = num;
        denominator = den;
        reduce();
    }

    // 약분 (기약분수 변환) 메서드
    void reduce() {
        int gcd = getGCD(numerator, denominator);
        numerator /= gcd;
        denominator /= gcd;
        
        // 분모의 부호를 항상 양수로 통일
        if (denominator < 0) {
            numerator = -numerator;
            denominator = -denominator;
        }
    }

    // 두 분수의 합을 연산하여 새로운 Fraction 객체를 반환하는 함수
    Fraction add(const Fraction& other) {
        // a/b + c/d = (ad + bc) / bd
        int newNum = (this->numerator * other.denominator) + (this->denominator * other.numerator);
        int newDen = this->denominator * other.denominator;
        
        return Fraction(newNum, newDen); // 생성자 내부에서 자동으로 reduce() 호출됨
    }

    // 출력 함수
    void print() const {
        std::cout << numerator << " / " << denominator << std::endl;
    }
};

int main() {
    std::cout << "--- Fraction 클래스 연산 테스트 ---" << std::endl;

    Fraction f1(2, 4); // 2/4 -> 생성 시 1/2로 약분됨
    std::cout << "분수 1: "; f1.print();

    Fraction f2(1, 3); // 1/3
    std::cout << "분수 2: "; f2.print();

    Fraction result = f1.add(f2); // 1/2 + 1/3 = 5/6
    std::cout << "연산 결과 (f1 + f2): "; result.print();

    return 0;
}