# 📅 2026-05-22 학습 요약: C++, 컴퓨터 비전, Linux

## 1. C++ 프로그래밍: 객체 지향 프로그래밍(OOP) 심화 및 정적 멤버

다양한 예제 파일(`app.cpp`, `circle.h`, `fraction.cpp`, `Prg7-x.cpp` 시리즈)을 통해 클래스의 멤버 접근 제어, 데이터 무결성 보장, 그리고 메모리를 공유하는 정적(static) 멤버의 활용법을 다루었습니다.

### ① 데이터 무결성 보장 및 정규화 (Data Invariants)
클래스가 설계된 목적에 맞는 올바른 값만을 가지도록 강제하는 방어적 코드 설계 패턴입니다.
* **`fraction.cpp` (분수 클래스)**: 분모가 `0`이 되는 치명적인 수학적 오류를 방지하기 위해 생성자 및 Setter 함수에서 `assert(false)` 기법을 사용하여 비정상 흐름을 원천 차단합니다. 또한, 연산이 일어날 때마다 내부 유틸리티 함수인 `normalize()`와 최대공약수(`gcd`) 계산 알고리즘을 호출하여 부호를 정리하고 항상 기약분수 형태를 유지합니다.
* **`Prg7-14.cpp` (시간 클래스)**: `tick()` 메서드를 호출하여 시간이 1초씩 증가할 때마다 내부적으로 `normalize()`가 구동됩니다. `60초 -> 1분 올림`, `60분 -> 1시간 올림`, `24시 -> 0시 초기화`를 수행하며 시, 분, 초 데이터의 결함이 없도록 정규화 규칙을 수행합니다.

### ② 정적(Static) 멤버를 활용한 자원 공유
일반 인스턴스 변수는 객체마다 독립된 메모리 공간을 가지지만, `static` 키워드가 붙은 멤버는 프로그램 시작 시 데이터 영역에 단 하나만 할당되어 모든 객체가 공유합니다.
* **살아있는 객체 카운팅 (`Prg7-5.cpp`)**: `Rectangle` 클래스 내부의 생성자들(`기본`, `매개변수`, `복사 생성자`)이 호출될 때마다 `count++`를 수행하고, `~Rectangle()` 소멸자가 호출될 때 `count--`를 처리하여 현재 메모리에 상주 중인 객체의 총개수를 실시간으로 추적합니다.
* **고유 일련번호 발급 (`Prg7-6.cpp`)**: `Account`(은행 계좌) 클래스에서 일련번호의 기준이 되는 기준 변수를 `static int base`로 지정합니다. 새로운 계좌 객체가 개설될 때마다 `base++` 연산을 거쳐 고유한 계좌번호(`100001`, `100002`...)를 중복 없이 안전하게 순차 발급하는 로직을 다룹니다.

### ③ 모듈화와 분할 컴파일
* 하나의 거대한 소스 코드에 클래스를 전부 밀어 넣던 방식에서 탈피하여, 클래스의 인터페이스(선언)는 헤더 파일(`.h`)에 작성하고 멤버 함수의 구체적인 로직(구현)은 소스 파일(`.cpp`)에 작성하여 최종 실행 파일(`app.cpp`)과 링크하는 멀티 파일 컴파일 프로젝트 구조를 실습했습니다.

---

## 2. 컴퓨터 비전: 딥러닝 CNN 모델 설계 및 전이 학습(Transfer Learning)

텐서플로(TensorFlow/Keras)와 OpenCV 파이썬 라이브러리를 활용하여 기본 신경망 구축부터 데이터 증대, 대형 모델을 활용한 전이 학습 파이프라인을 다루었습니다.

### ① 데이터 증대 (Data Augmentation)
* **`8-5.py`**: 학습용 이미지 데이터셋이 양적으로 불충분하거나 특정 방향에 편향되어 있을 때 알고리즘의 오버피팅을 방지하기 위한 필수 기법입니다.
* `ImageDataGenerator`를 사용해 원본 이미지를 무작위로 회전(`rotation_range=20.0`), 평행 이동(`width/height_shift_range=0.2`), 좌우 반전(`horizontal_flip=True`)시켜 데이터 세트를 인위적으로 풍부하게 확장하여 어떤 환경에서도 객체를 잘 찾아내는 강인한 모델을 학습시킵니다.

### ② 사전 학습 모델과 전이 학습 (Transfer Learning)
처음부터 거대한 가중치를 학습시키는 대신, 대형 데이터셋(ImageNet)으로 이미 똑똑하게 학습이 완료된 대기업의 인공지능 모델 체계를 베이스 모델로 이식받아 내 임무에 맞게 재가공하는 고급 기법입니다.



* **`8-6.py` (ResNet50 기본 추론)**: 내장된 거대 신경망 `ResNet50` 모델을 즉시 호출하고, OpenCV로 읽어들인 일반 사물 이미지(`rabbit.jpg`)를 모델 입력 규격(`224x224x3`)으로 전처리(`preprocess_input`)한 후 예측하여 Top-5 확률을 화면에 시각화했습니다.
* **`8-7.py` (DenseNet121 기반 파인 튜닝)**: 스탠포드 견종 데이터셋(`stanford_dogs`)을 분류하기 위해 `DenseNet121` 신경망을 가져왔습니다. 고차원 특징 추출용 하위 레이어의 가중치는 보존하고, 분류 모델 최상위의 완전연결층(`Dense Layer`)을 날려버린 후(`include_top=False`), 우리가 타겟으로 하는 **120가지 품종 분류**에 맞게 커스텀 분류기(Dense 및 Dropout 층 링킹)를 새로 부착하여 미세 조정(Fine-tuning)하는 매커니즘을 실습했습니다.

### ③ 딥러닝 인공지능 시스템의 GUI 통합 애플리케이션 (`8-8.py`)
* 백엔드에서 작동하는 전이학습 견종 인식 모델(`cnn_for_stanford_dogs.h5`)과 견종 매핑 데이터(`dog_species_names.txt`)를 GUI 프레임워크인 **PyQt5**의 이벤트 기반 프로그래밍과 유기적으로 바인딩했습니다.
* 사용자가 창에서 버튼을 클릭해 사진을 불러오면(`QFileDialog`) 인공지능 모델이 즉각 예측을 수행하여 가장 점수가 높은 상위 5개의 견종 이름과 예측 확률 수치 결과를 인터페이스 창에 직관적으로 뿌려주는 종합 완성형 데모 애플리케이션을 완성했습니다.

---

## 3. 우분투 리눅스: 네트워크 인터페이스 상태 점검 및 공유 서버(Samba) 구축

### ① 도메인 네임 질의 및 파일 수신 유틸리티
* **`nslookup [도메인 주소]`**: DNS(Domain Name System) 서버에 질의하여 사용자가 입력한 문자 도메인 정보와 매핑된 실제 네트워크 물리 IP 주소를 확인 및 추적합니다.
* **`curl` / `wget`**: GUI 브라우저가 제공되지 않는 순수 CLI 터미널 환경에서 HTTP/HTTPS 프로토콜을 이용해 외부 웹 서버로 패킷 요청을 날려 통신 상태 헤더를 수집하거나, 인터넷상의 패키지/오픈소스 압축 리소스 파일을 백그라운드로 즉시 내려받을 때 다용도로 응용됩니다.

### ② 이기종 파일 공유 시스템 삼바(Samba) 서버 구축
우분투 리눅스 개발 환경에 존재하는 비전 검사 이미지 데이터 폴더나 C++ 소스코드 디렉토리를 사무실 내의 윈도우(Windows) 클라이언트 PC에서 로컬 폴더처럼 손쉽게 접근하고 네트워크 드라이브로 마운트할 수 있도록 하는 파일 공유 서버 환경 설정 실무입니다.

1. **패키지 설치 및 디렉토리 권한 개방**:
   ```bash
   sudo apt update && sudo apt install samba
   sudo mkdir -p /sambawin
   sudo chmod 777 /sambawin

### ② 삼바(Samba) 서버 구축을 통한 크로스 플랫폼 파일 공유
리눅스 개발 환경 내부의 소스코드나 비전 검사 이미지 저장소 폴더를 사무실의 윈도우(Windows) 클라이언트 PC에서 네트워크 드라이브로 마운트하여 실시간 공유할 수 있도록 서버를 설정합니다.

1. **패키지 설치 및 디렉토리 권한 개방**:
```bash
   sudo apt update && sudo apt install samba
   sudo mkdir -p /sambawin
   sudo chmod 777 /sambawin
```

```bash
[sambawin]
   comment = Windows Shared Directory
   path = /sambawin
   browseable = yes
   writable = yes
   guest ok = no
   valid users = @smbgroup, user1  # (옵션) 특정 그룹이나 사용자만 접근 허용 시 추가
```


2. **삼바 메인 환경 설정 수정 (/etc/samba/smb.conf)**:
파일 맨 하단에 공유할 절대 경로, 공유 디렉토리 네이밍, 읽기/쓰기 활성화 권한 및 접근 허용 조건을 선언합니다.


3. **삼바 전용 유저 암호 생성 및 자격 증명 연동**:
리눅스 기존 시스템 계정을 삼바 접근 보안 유저로 동기화하고 독립적인 암호를 부여합니다.

4. **삼바 서비스 데몬 재시작 및 활성화**:
수정한 설정 파일(smb.conf)을 시스템에 적용하기 위해 삼바 핵심 프로세스 데몬을 리스타트합니다.

```bash
sudo systemctl restart smbd
sudo systemctl enable smbd  # 부팅 시 자동 실행 설정
```

5. **윈도우(Windows) 클라이언트 최종 연결 검증**:

윈도우 PC에서 Win + R 키를 눌러 실행 창을 켭니다.

주소창에 \\[리눅스 서버 IP 주소]\sambawin을 입력하고 엔터를 누릅니다.

네트워크 자격 증명 창이 뜨면 3단계에서 등록한 삼바 유저 아이디와 패스워드를 입력하여 네트워크 드라이브 마운트를 완수합니다.