# 📅 2026-05-13 - 자료구조 (파이썬) 

# 트리와 이진트리 정리

## 1. 트리(Tree)란?

트리는 **계층적(Hierarchical) 구조**를 표현하는 비선형 자료구조입니다.  
데이터가 부모-자식 관계로 연결되며, 사이클이 없는 구조를 가집니다.

예를 들어 파일 시스템, 조직도, HTML DOM 구조 등이 트리 형태로 표현됩니다.

---

## 2. 트리의 기본 용어

- **노드(Node)**: 트리의 각 데이터 요소
- **루트 노드(Root Node)**: 트리의 시작이 되는 최상위 노드
- **부모 노드(Parent Node)**: 어떤 노드의 바로 위에 있는 노드
- **자식 노드(Child Node)**: 어떤 노드의 바로 아래에 있는 노드
- **형제 노드(Sibling Node)**: 같은 부모를 가지는 노드
- **리프 노드(Leaf Node)**: 자식이 없는 노드
- **서브트리(Subtree)**: 특정 노드를 루트로 하는 작은 트리
- **깊이(Depth)**: 루트에서 특정 노드까지의 거리
- **높이(Height)**: 특정 노드에서 가장 깊은 리프까지의 거리
- **레벨(Level)**: 같은 깊이를 가지는 노드들의 층

예시:

```text
        A
      / | \
     B  C  D
    / \
   E   F
```

- 루트 노드: `A`
- `A`의 자식 노드: `B`, `C`, `D`
- `B`의 자식 노드: `E`, `F`
- 리프 노드: `C`, `D`, `E`, `F`

---

## 3. 트리의 특징

- 계층 구조를 표현하기에 적합합니다.
- 노드가 여러 개의 자식을 가질 수 있습니다.
- 일반적으로 **사이클이 없습니다**.
- 노드 수가 `n`개이면 간선 수는 `n-1`개입니다.

---

## 4. 트리의 종류

### 4-1. 일반 트리(General Tree)

각 노드가 가질 수 있는 자식 수에 제한이 없는 트리입니다.

### 4-2. 이진트리(Binary Tree)

각 노드가 **최대 2개의 자식 노드**를 가지는 트리입니다.  
왼쪽 자식과 오른쪽 자식이 구분됩니다.

### 4-3. 이진 탐색 트리(Binary Search Tree)

이진트리의 한 종류로, 왼쪽 서브트리에는 작은 값, 오른쪽 서브트리에는 큰 값이 들어가도록 구성됩니다.

### 4-4. 힙(Heap)

완전 이진트리 기반의 자료구조로, 우선순위 큐 구현에 사용됩니다.

---

## 5. 트리의 순회(Tree Traversal)

트리의 모든 노드를 방문하는 방법을 순회라고 합니다.

### 5-1. 전위 순회(Preorder)

1. 현재 노드 방문
2. 자식 노드 순서대로 방문

### 5-2. 후위 순회(Postorder)

1. 자식 노드 방문
2. 현재 노드 방문

### 5-3. 레벨 순회(Level Order)

위에서 아래로, 같은 레벨에서는 왼쪽부터 방문

---

## 6. 간단한 트리 구조 예시(파이썬)

```python
class TreeNode:
    def __init__(self, data):
        self.data = data
        self.children = []

root = TreeNode('A')
b = TreeNode('B')
c = TreeNode('C')
d = TreeNode('D')
e = TreeNode('E')
f = TreeNode('F')

root.children = [b, c, d]
b.children = [e, f]
```

---

## 7. 이진트리(Binary Tree)란?

이진트리는 각 노드가 **최대 2개의 자식 노드**를 가지는 트리입니다.  
자식은 보통 다음과 같이 구분합니다.

- **왼쪽 자식(Left Child)**
- **오른쪽 자식(Right Child)**

예시:

```text
        A
       / \
      B   C
     / \   \
    D   E   F
```

---

## 8. 이진트리의 특징

- 각 노드는 최대 두 개의 자식을 가집니다.
- 왼쪽과 오른쪽 자식이 구분됩니다.
- 재귀적으로 정의할 수 있습니다.
- 탐색, 정렬, 우선순위 처리 등에 자주 활용됩니다.

---

## 9. 이진트리의 종류

### 9-1. 포화 이진트리(Full Binary Tree)

모든 노드가 자식이 0개이거나 2개인 트리입니다.

### 9-2. 완전 이진트리(Complete Binary Tree)

마지막 레벨을 제외한 모든 레벨이 가득 차 있고, 마지막 레벨은 왼쪽부터 채워지는 트리입니다.

### 9-3. 정이진트리(Perfect Binary Tree)

모든 내부 노드가 두 자식을 가지고, 모든 리프 노드의 깊이가 같은 트리입니다.

### 9-4. 편향 이진트리(Skewed Binary Tree)

모든 노드가 한쪽 자식만 가지는 형태의 트리입니다.

---

## 10. 파이썬으로 이진트리 구현

```python
class Node:
    def __init__(self, data):
        self.data = data
        self.left = None
        self.right = None

root = Node('A')
root.left = Node('B')
root.right = Node('C')
root.left.left = Node('D')
root.left.right = Node('E')
root.right.right = Node('F')
```

---

## 11. 이진트리 순회

### 11-1. 전위 순회(Preorder)

순서:
1. 현재 노드
2. 왼쪽 서브트리
3. 오른쪽 서브트리

```python
def preorder(node):
    if node:
        print(node.data, end=' ')
        preorder(node.left)
        preorder(node.right)
```

### 11-2. 중위 순회(Inorder)

순서:
1. 왼쪽 서브트리
2. 현재 노드
3. 오른쪽 서브트리

```python
def inorder(node):
    if node:
        inorder(node.left)
        print(node.data, end=' ')
        inorder(node.right)
```

### 11-3. 후위 순회(Postorder)

순서:
1. 왼쪽 서브트리
2. 오른쪽 서브트리
3. 현재 노드

```python
def postorder(node):
    if node:
        postorder(node.left)
        postorder(node.right)
        print(node.data, end=' ')
```

### 11-4. 레벨 순회(Level Order)

```python
from collections import deque

def level_order(root):
    if not root:
        return

    queue = deque([root])

    while queue:
        node = queue.popleft()
        print(node.data, end=' ')

        if node.left:
            queue.append(node.left)
        if node.right:
            queue.append(node.right)
```

---

## 12. 이진트리 주요 연산

### 12-1. 노드 개수 세기

```python
def count_nodes(node):
    if node is None:
        return 0
    return 1 + count_nodes(node.left) + count_nodes(node.right)
```

### 12-2. 트리 높이 구하기

```python
def tree_height(node):
    if node is None:
        return 0
    return 1 + max(tree_height(node.left), tree_height(node.right))
```

### 12-3. 리프 노드 개수 세기

```python
def count_leaf_nodes(node):
    if node is None:
        return 0
    if node.left is None and node.right is None:
        return 1
    return count_leaf_nodes(node.left) + count_leaf_nodes(node.right)
```

---

## 13. 전체 예제 코드

```python
from collections import deque

class Node:
    def __init__(self, data):
        self.data = data
        self.left = None
        self.right = None

def preorder(node):
    if node:
        print(node.data, end=' ')
        preorder(node.left)
        preorder(node.right)

def inorder(node):
    if node:
        inorder(node.left)
        print(node.data, end=' ')
        inorder(node.right)

def postorder(node):
    if node:
        postorder(node.left)
        postorder(node.right)
        print(node.data, end=' ')

def level_order(root):
    if not root:
        return

    queue = deque([root])
    while queue:
        node = queue.popleft()
        print(node.data, end=' ')

        if node.left:
            queue.append(node.left)
        if node.right:
            queue.append(node.right)

def count_nodes(node):
    if node is None:
        return 0
    return 1 + count_nodes(node.left) + count_nodes(node.right)

def tree_height(node):
    if node is None:
        return 0
    return 1 + max(tree_height(node.left), tree_height(node.right))

def count_leaf_nodes(node):
    if node is None:
        return 0
    if node.left is None and node.right is None:
        return 1
    return count_leaf_nodes(node.left) + count_leaf_nodes(node.right)

root = Node('A')
root.left = Node('B')
root.right = Node('C')
root.left.left = Node('D')
root.left.right = Node('E')
root.right.right = Node('F')

print("전위 순회:", end=' ')
preorder(root)
print()

print("중위 순회:", end=' ')
inorder(root)
print()

print("후위 순회:", end=' ')
postorder(root)
print()

print("레벨 순회:", end=' ')
level_order(root)
print()

print("전체 노드 수:", count_nodes(root))
print("트리 높이:", tree_height(root))
print("리프 노드 수:", count_leaf_nodes(root))
```

---

## 14. 시간 복잡도

이진트리에서 노드 수를 `n`이라고 하면:

- 전위 순회: `O(n)`
- 중위 순회: `O(n)`
- 후위 순회: `O(n)`
- 레벨 순회: `O(n)`
- 노드 개수 세기: `O(n)`
- 트리 높이 구하기: `O(n)`

---

## 15. 트리와 이진트리의 차이

| 구분 | 트리 | 이진트리 |
|------|------|----------|
| 자식 수 | 제한 없음 | 최대 2개 |
| 자식 구분 | 보통 구분 없음 | 왼쪽/오른쪽 구분 |
| 구조 | 일반적 계층 구조 | 더 제한된 구조 |
| 활용 | 조직도, 디렉터리 | BST, Heap, Expression Tree |

---

## 16. 활용 분야

### 트리 활용
- 파일 시스템
- 조직도
- HTML/XML 문서 구조
- 카테고리 구조

### 이진트리 활용
- 이진 탐색 트리(BST)
- 힙(Heap)
- 수식 트리(Expression Tree)
- 허프만 트리(Huffman Tree)
- 세그먼트 트리, 펜윅 트리 등의 기반 개념 이해

---

## 17. 이진트리 구현 예제

완전 이진트리

```python
class TNode:
    def __init__(self, data, left=None, right=None):
        self.data = data
        self.left = left
        self.right = right


def inorder(node):
    if node is not None:
        inorder(node.left)
        print(node.data, end=' ')
        inorder(node.right)


def preorder(node):
    if node is not None:
        print(node.data, end=' ')
        preorder(node.left)
        preorder(node.right)


def postorder(node):
    if node is not None:
        postorder(node.left)
        postorder(node.right)
        print(node.data, end=' ')


def levelorder(root):
    if root is None:
        return

    queue = [root]

    while queue:
        node = queue.pop(0)
        print(node.data, end=' ')

        if node.left is not None:
            queue.append(node.left)
        if node.right is not None:
            queue.append(node.right)


def count_node(node):
    if node is None:
        return 0
    return 1 + count_node(node.left) + count_node(node.right)


def count_leaf(node):
    if node is None:
        return 0
    if node.left is None and node.right is None:
        return 1
    return count_leaf(node.left) + count_leaf(node.right)


def calc_height(node):
    if node is None:
        return 0
    return 1 + max(calc_height(node.left), calc_height(node.right))


# 완전 이진트리 생성
d = TNode('D', None, None)
e = TNode('E', None, None)
b = TNode('B', d, e)
f = TNode('F', None, None)
c = TNode('C', f, None)
root = TNode('A', b, c)


print("In-Order   : ", end='')
inorder(root)
print()

print("Pre-Order  : ", end='')
preorder(root)
print()

print("Post-Order : ", end='')
postorder(root)
print()

print("Level-Order: ", end='')
levelorder(root)
print()

print("노드의 개수 =", count_node(root))
print("단말의 개수 =", count_leaf(root))
print("트리의 높이 =", calc_height(root))



```

## 18. 힙(heap) 

## 19. 핵심 요약

- **트리**는 계층 구조를 표현하는 비선형 자료구조입니다.
- **이진트리**는 각 노드가 최대 2개의 자식을 가지는 트리입니다.
- 트리는 일반적인 개념이고, 이진트리는 그중 하나의 특수한 형태입니다.
- 이진트리에서는 순회와 재귀가 매우 중요합니다.
- 이진 탐색 트리, 힙 등 다양한 고급 자료구조의 기반이 됩니다.
