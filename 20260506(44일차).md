# 📅 2026-05-06 - 자료구조 (복습)

## 개요

### 1. 선형 자료구조
자료구조(data structure)란?
자료구조는 데이터를 효율적으로 저장하고 관리하고 사용할 수 있도록 조직하는 방식입니다.

쉽게 말하면,
같은 데이터라도 어떻게 담고 꺼내느냐에 따라 프로그램의 성능과 구현 방식이 크게 달라지는데,
그 “데이터를 담는 규칙과 형태”가 바로 자료구조입니다.

### 왜 중요한가?

- 자료구조를 잘 선택하면?
- 삽입/삭제가 쉬워짐
- 메모리를 더 효율적으로 쓸 수 있음
- 프로그램 전체 성능이 좋아짐

### 예시

- 배열(Array): 번호 순서대로 정리된 사물함
- 스택(Stack): 접시를 쌓아두는 구조(나중에 넣은 것을 먼저 꺼냄)
- 큐(Queue) : 줄 서기 구조(먼저 온 것들을 먼저 처리)
- 트리(Tree): 폴더 구조처럼 계층적으로 정리
- 그래프(Graph) : 지하철 노선처럼 여러점이 연결된 구조
- 해시 테이블(Hash Table) : 사전처럼 빠르게 찾는 구조

### 자료구조의 목적

- 탐색을 빠르게 하기 위해 쓰임
- 정렬/처리를 쉽게 하기 위해 쓰임
- 삽입/삭제를 효율적으로 하기 위해 쓰임
- 데이터 관계를 잘 표현하기 위해서 쓰임

### 한줄 정의

자료구조는 데이터를 컴퓨터 안에서 효율적으로 저장하고 처리하기 위한 조직 방법


### 2. 알고리즘 정리

#### (1) 알고리즘이란?
알고리즘(Algorithm)은 **문제를 해결하기 위한 절차, 방법, 명령어들의 집합**이다.  
즉, 어떤 문제를 해결하기 위해 입력을 받아 원하는 결과를 얻기까지의 **명확하고 순서 있는 과정**을 의미한다.

---

#### (2) 알고리즘의 조건
알고리즘은 다음 조건을 만족해야 한다.

- **입력(Input)**: 0개 이상의 입력이 있어야 한다.
- **출력(Output)**: 1개 이상의 결과가 나와야 한다.
- **명확성(Definiteness)**: 각 단계가 모호하지 않고 분명해야 한다.
- **유한성(Finiteness)**: 반드시 유한한 단계 후에 종료되어야 한다.
- **유효성(Effectiveness)**: 각 단계가 실제로 수행 가능해야 한다.

---

#### (3) 알고리즘의 기술 방법
알고리즘은 여러 가지 방법으로 표현할 수 있다.

- **자연어**: 일상적인 문장으로 설명하는 방법
- **의사코드(Pseudocode)**: 프로그래밍 언어와 비슷하게 간단히 표현하는 방법
- **순서도(Flowchart)**: 도형과 화살표로 흐름을 나타내는 방법
- **프로그래밍 언어**: Python, C, Java 등 실제 코드로 작성하는 방법

---

#### (4) 요약
- 알고리즘: **문제를 해결하기 위한 절차적 방법**
- 조건: **입력, 출력, 명확성, 유한성, 유효성**
- 기술 방법: **자연어, 의사코드, 순서도, 프로그래밍 언어**

---

#### (5). 짧은 답안형
> 알고리즘은 문제를 해결하기 위한 명확하고 유한한 절차 또는 방법이다.  
> 알고리즘의 조건에는 입력, 출력, 명확성, 유한성, 유효성이 있다.  
> 알고리즘의 기술 방법에는 자연어, 의사코드, 순서도, 프로그래밍 언어가 있다.

---

### 3. 추상형 자료형(ADT, Abstract Data Type)

추상형 자료형은 **데이터와 그에 대한 연산을 추상적으로 정의한 자료형**이다.  
즉, **무엇을 할 수 있는지(인터페이스)**만 제공하고, **어떻게 구현되는지(구현 방식)**는 숨긴다.

#### 특징
- 인터페이스만 공개한다.
- 내부 구현은 은닉한다.
- 같은 ADT를 여러 방식으로 구현할 수 있다.
- 코드의 재사용성과 유지보수성이 높아진다.

#### 예시
스택(Stack) ADT는 다음 연산을 가진다.
- `push(x)`: 원소 삽입
- `pop()`: 원소 삭제
- `peek()`: 맨 위 원소 확인
- `isEmpty()`: 공백 여부 확인

이 스택은 배열이나 연결 리스트, 클래스 등 여러 방법으로 구현할 수 있다.

#### 한 줄 정의
> 추상형 자료형은 데이터와 연산을 추상적으로 정의하고, 구현은 숨기는 자료형이다.

---

### 4. 알고리즘 성능을 볼때 주요 기준
알고리즘 성능은 보통 아래 3가지로 많이 평가합니다.


#### (1) 계산 속도
- 계산 속도
  - 입력을 처리하는데 걸리는 시간
  - 보통 실행 시간, 시간 복잡도로 봅니다.
  - 예 :  **O(n)**, **O(n log n)**, **O(n^2)**

#### (2) 메모리 사용량

- 알고리즘이 실행 중 얼마나 많은 메모리를 쓰는지
- 보통 공간 복잡도로 표현합니다
- 속도는 빠르지만 메모리를 많이 쓰는 알고리즘도 있고, 반대도 있습니다.

#### (3) 기능 달성도

- 원하는 문제를 정확하게 해결하는지
- 단순히 빠른 것보다 정답률, 안정성, 예외 처리가 더 중요 할 수 있습니다.
- 실제 서비스에서는 속도보다 기능 충족이 우선인 경우도 있습니다.

### 4-1. 시간 측정이 어려운 이유
알고리즘의 실행 시간은 생각보다 정확히 비교하기 어렵습니다.

- 컴퓨터 성능이 다름
- CPU, 메모리, 운영체제 영향
- 같은 알고리즘도 구현 방식에 따라 다름
- 입력 데이터의 크기와 형태에 따라 달라짐
- 실행할 때마다 미세한 오차가 생김

```python

import time

myBag = []
start = time.time()
insert(myBag, '축구공')

end = time.time()
print("실행시간= " , end-start)

```

---

### 5. 알고리즘 성능: 복잡도 분석
알고리즘의 성능은 보통 복잡도 분석(complexity analysis) 으로 평가합니다.
복잡도 분석은 알고리즘이 문제를 해결할 때 필요한 시간과 메모리가 입력 크기에 따라 어떻게 증가하는지를 분석하는 방법입니다.

#### 1. 시간 복잡도
시간 복잡도는 입력 크기 **n** 커질수록 연산 횟수가 어떻게 변하는지를 나타냅니다.

#### 예시
- `O(1)` : 항상 일정한 시간
- `O(log n)` : 로그에 비례
- `O(n)` : 입력 크기에 비례
- `O(n log n)` : 정렬 알고리즘에서 자주 나타남
- `O(n^2)` : 이중 반복문에서 자주 나타남

실행 시간을 직접 입력하는 대신 입력증가에 따른 처리량 변화를 기준으로
성능을 비교합니다.

### 2. 공간 복잡도
공간 복잡도는 알고리즘이 실행될 때 필요한 메모리 사용량을 나타냅니다.

#### 예시

- `O(1)` : 추가 메모리를 거의 사용하지 않음
- `O(n)` : 입력 크기만큼 추가 저장 공간 필요
- `O(n^2)` : 2차원 배열 등 큰 메모리 필요

시간이 빠르더라도 메모리를 많이 쓰면 비효율적일 수 있으므로,
시간 복잡도와 함께 고려해야 합니다.

### 3. 복잡도 분석의 장점
복잡도 분석은 실제 실행 환경에 크게 의존하지 않고
알고리즘의 이론적 효율성을 비교할 수 있다는 장점이 있습니다.

실행 시간 측정은 다음 요인에 따라 달라질 수 있습니다.

- 하드웨어 성능
- 운영체제 환경
- 프로그래밍 언어
- 구현 방식
- 입력 데이터 형태

따라서 절대적인 실행 시간보다 복잡도 분석이 알고리즘 성능 평가의 핵심 기준이 됩니다.

### 정리
알고리즘 성능은 주로 복잡도 분석으로 평가하며 이는 다음 두 가지로 나뉩니다.

시간 복잡도: 연산 시간이 얼마나 증가하는가
공간 복잡도: 메모리 사용량이 얼마나 증가하는가
복잡도 분석은 알고리즘의 계산 속도와 메모리 효율성을 체계적으로 평가하는 방법입니다.

### 한 줄 요약
알고리즘 성능은 입력 크기에 따른 시간과 공간의 증가 정도를 분석하는 복잡도 분석으로 평가한다.

리스트(list) / 선형 리스트(linear list)