# 📅 2026-05-26 - 자바

## 비즈니스 로직 처리

## 1. 데이터 베이스
데이터베이스(Database)는 데이터를 체계적으로 저장하고 관리하는 시스템입니다. 이를 통해 데이터를 효율적으로 저장, 검색, 수정, 삭제할 수 있습니다. 대표적인 DBMS로는 오라클(Oracle), MySQL, PostgreSQL, SQL Server 등이 있습니다.


## 2. 오라클 DBMS 특징
- **확장성과 성능** : 대용량 데이터베이스 처리 및 응답시간 향상을 위한 강력한
기능을 제공합니다.
- **고가용성 및 안정성** : 데이터 손실을 방지하기 위한 백업 및 복구 기능을 제공하고, 시스템 장애 시에도 빠른 복구 지원
- **보안** : 데이터베이스 보안 강화를 위한 다양한 기능을 제공하여 데이터 무결성과 보안을 유지합니다.
- ** 다양한 기능 : 오라클은 다양한 데이터 모델링, 질의 언어 및 응용 프로그램 개발을 위한 기능 제공합니다.


## 3. 비즈니스 로직 처리 및 웹 연동 구조
소스 코드는 자바 웹 애플리케이션의 클래식한 구조인 Servlet - DAO - VO 패턴을 따르고 있습니다. 사용자가 웹 브라우저를 통해 요청을 보내면 서블릿이 요청을 받아 데이터베이스에서 회원 정보를 조회한 뒤, HTML 테이블 형태로 동적 화면을 구성하여 응답하는 흐름을 가집니다.

### 3-1 데이터베이스 및 웹 연동 개념

- 데이터베이스 (Database)
데이터를 체계적으로 저장하고 관리하는 시스템입니다. 데이터를 효율적으로 CRUD(생성, 조회, 수정, 삭제)할 수 있도록 돕습니다. 본 프로젝트에서는 대규모 데이터 처리와 엔터프라이즈 환경에서 안정성이 검증된 오라클(Oracle) DBMS를 사용하고 있습니다.

- JDBC (Java Database Connectivity) 방식의 특징
현재 업로드된 MemberDAO 코드는 순수 JDBC 드라이버 매니저를 이용해 데이터베이스에 접근하고 있습니다.

동작 방식: 자바 프로그램이 구동 환경에 맞는 데이터베이스 드라이버(oracle.jdbc.driver.OracleDriver)를 동적으로 로드한 뒤, 연결 문자열(URL)과 계정 정보를 기반으로 물리적인 커넥션 객체(Connection)를 직접 생성합니다.

특징 및 한계: 구현이 직관적이지만, 사용자의 요청이 들어올 때마다 DriverManager.getConnection()을 통해 데이터베이스와 매번 새로운 네트워크 연결을 수립하고 차단(con.close())해야 합니다. 이 방식은 대규모 동시 접속자가 몰리는 상용 웹 서비스 환경에서는 서버 자원 소모(오버헤드)가 매우 커지는 원인이 됩니다.

| 파일명 | 주요역할 | 핵심 메커니즘 |
| --- | --- | --- |
| MemberServlet.java | 컨트롤러 (Controller) / 뷰 (View) 역할 겸임 | @WebServlet("/member") 매핑doGet() 내부에서 HTML 문자열 스트림(PrintWriter) 출력 |
| MemberDAO.java | 데이터 접근 객체 (Data Access Object) | JDBC API (Connection, Statement, ResultSet) 활용connDB() 호출 |
| MemberVO.java | 값 객체 (Value Object) / 데이터 전송 객체 (DTO) | Getter / Setter 활용java.sql.Date 타입을 활용한 데이터 보관


## 4. MemberVO.java (Value Object)
회원 테이블 구조에 맞춘 필드 선언 및 캡슐화(Encapsulation) 데이터 객체입니다.

```java
package sec01.ex01;

import java.sql.Date;

public class MemberVO {
	private String id;
	private String pwd;
	private String name;
	private String email;
	private Date joinDate; // 데이터베이스의 DATE 타입과 매핑
	
	public MemberVO() {
		System.out.println("MemberVO 생성자 호출");
	}

	public String getId() { return id; }
	public void setId(String id) { this.id = id; }

	public String getPwd() { return pwd; }
	public void setPwd(String pwd) { this.pwd = pwd; }

	public String getName() { return name; }
	public void setName(String name) { this.name = name; }

	public String getEmail() { return email; }
	public void setEmail(String email) { this.email = email; }

	public Date getJoinDate() { return joinDate; }
	public void setJoinDate(Date joinDate) { this.joinDate = joinDate; }
}


```

## 5. MemberDAO.java (Data Access Object)

```java
package sec01.ex01;

import java.sql.Connection;
import java.sql.Date;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.ArrayList;
import java.util.List;

public class MemberDAO {
	
	private Connection con;
	private Statement stmt;
	
	// 오라클 데이터베이스 연결 정보 설정 (@ 주소 구분 및 대소문자 매칭 완비)
	private static final String driver = "oracle.jdbc.driver.OracleDriver";
	private static final String url = "jdbc:oracle:thin:@localhost:1521:testdb";
	private static final String user = "aisw";
	private static final String pwd = "tiger";
	
	public List listMembers() {
		List list = new ArrayList();
		
		try {
			connDB(); // 내부 DB 연결 메서드 호출
			String query = "select * from t_member";
			System.out.println(query);
			
			ResultSet rs = stmt.executeQuery(query);
			
			while(rs.next()) {
				String id = rs.getString("id");
				String pwd = rs.getString("pwd");
				String name = rs.getString("name");
				String email = rs.getString("email");
				Date joinDate = rs.getDate("joinDate"); // VO의 타입 정의에 맞춰 getDate 사용
				
				MemberVO vo = new MemberVO();
				vo.setId(id);
				vo.setPwd(pwd);
				vo.setName(name);
				vo.setEmail(email);
				vo.setJoinDate(joinDate);
				
				list.add(vo); // 컬렉션 리스트에 VO 객체 적재
			}
			
			// 역순 자원 반납
			rs.close();
			stmt.close();
			con.close();
		} catch(Exception e) {
			e.printStackTrace();
		}
		return list;
	}
	
	// 물리적인 커넥션을 수립하는 내부 프라이빗 메서드
	private void connDB() {
		try {
			Class.forName(driver);
			System.out.println("Oracle 드라이버 로딩 성공");
			
			con = DriverManager.getConnection(url, user, pwd);
			System.out.println("Connection 생성 성공");
			
			stmt = con.createStatement();
			System.out.println("Statement 생성 성공");
			
		} catch(Exception e) {
			e.printStackTrace();
		}
	}
}


```


## 6. MemberServlet.java (Servlet 컨트롤러 및 뷰 구현)

```java
package sec01.ex01;

import java.io.IOException;
import java.io.PrintWriter;
import java.sql.Date;
import java.util.List;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/member") // 브라우저 주소창 매핑 URL 지정
public class MemberServlet extends HttpServlet {
	
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		// 응답 문서의 인코딩 타입 및 한글 깨짐 방지 설정
		response.setContentType("text/html;charset=utf-8");
		PrintWriter out = response.getWriter();
		
		// 비즈니스 로직 객체 생성 및 데이터 조회
		MemberDAO dao = new MemberDAO();
		List list = dao.listMembers();
		
		// 동적 웹 페이지 생성 (HTML 구조 출력)
		out.print("<html><body>");
		out.print("<table border=1><tr align='center' bgcolor='lightgreen'>");
		out.print("<td>아이디</td><td>비밀번호</td><td>이름</td><td>이메일</td><td>가입일</td></tr>");
		
		// 루프를 돌며 가입된 회원 레코드를 한 행씩 출력
		for (int i=0; i<list.size(); i++) {
			MemberVO memberVO = (MemberVO) list.get(i); // 다형성 다운캐스팅
			String id = memberVO.getId();
			String pwd = memberVO.getPwd();
			String name = memberVO.getName();
			String email = memberVO.getEmail();
			Date joinDate = memberVO.getJoinDate();
			
			out.print("<tr><td>" + id + "</td><td>" + pwd + "</td><td>"
					+ name + "</td><td>" + email + "</td><td>" + joinDate + "</td></tr>");
		}
		out.print("</table></body></html>");
	}
}

```