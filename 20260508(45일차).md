# 📅 2026-05-08 - C++, 컴퓨터비전, linux

## 개요

### 45일차 - C++ 함수와 포인터
- 함수에 값을 전달하는 방식과 주소를 전달하는 방식 학습
- 포인터를 이용하면 함수 내부에서 원본 변수 값을 변경할 수 있음
- * 연산자는 포인터가 가리키는 실제 값을 의미
- & 연산자는 변수의 주소를 의미

### 1. C++ (중급)

```c++

#include <iostream>
using namespace std;

void fun(int *y)
{
    *y = 20;
}

int main()
{
    int x = 10;

    fun(&x);

    cout << x;

    return 0;
}

```

#### 핵심 내용

- fun(&x) : 변수 x의 주소 전달
- *y = 20 : x의 실제 값 변경
- 출력 결과 : 20

### 2. 함수 오버로딩

- 같은 이름의 함수를 여러 개 정의하는 기능
- 매개변수의 개수나 자료형이 달라야 함
- 함수 이름은 같지만 상황에 따라 다른 함수가 호출됨

```c++
#include <iostream>
using namespace std;

void sum(int a, int b)
{
    cout << a + b << endl;
}

void sum(double a, double b)
{
    cout << a + b << endl;
}

int main()
{
    sum(10, 20);
    sum(3.5, 2.1);

    return 0;
}

```

#### 핵심내용
- sum(10, 20) → int 함수 호출
- sum(3.5, 2.1) → double 함수 호출
- 함수 이름 재사용 가능


### ３. 포인터 기본 개념

포인터 특징
- 다른 변수의 주소를 저장하는 변수
- 메모리 주소를 직접 다룰 수 있음

```C++
int x = 10;
int *p = &x;
```

#### ✅ 의미
- x : 일반 변수
- &x : x의 주소값
- p : x의 주소 저장
- *p : x의 실제 값 접근


#### 학습 핵심 정리
- & : 주소 연산자
- * : 참조(역참조) 연산자
- 포인터를 사용하면 원본 데이터 수정 가능
- 함수 오버로딩은 매개변수 형태가 달라야 함
- C++은 메모리 주소를 활용한 프로그래밍이 가능함
