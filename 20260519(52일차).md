# 📅 2026-05-19 - Java

# 서블릿 필수 정리

## 1. 서블릿이란?

서블릿(Servlet)은 **클라이언트의 요청을 받아 처리하고, 그 결과를 응답으로 보내는 자바 기반 서버 프로그램**입니다.  
즉, 웹 브라우저의 요청을 받아 서버에서 동작하는 자바 클래스입니다.

---

## 2. 서블릿의 핵심 역할

서블릿은 크게 아래 3가지를 수행합니다.

### 1) 요청(Request) 받기
클라이언트가 보낸 데이터를 읽습니다.

- 사용 클래스: `HttpServletRequest`

예:
- 사용자가 보낸 파라미터 읽기
- 요청 방식(GET/POST) 확인
- 브라우저 정보, IP 확인

### 2) 응답(Response) 보내기
처리 결과를 브라우저에 전달합니다.

- 사용 클래스: `HttpServletResponse`

예:
- HTML 출력
- 문자 인코딩 설정
- 상태 코드 지정
- 리다이렉트 처리

### 3) 생명주기(Lifecycle) 따라 동작
서블릿은 컨테이너가 생성하고 관리합니다.

주요 메서드:
- `init()` : 서블릿 초기화, 최초 1번 호출
- `service()` : 요청 처리
- `destroy()` : 종료 직전 1번 호출

실무에서는 보통 `service()`를 직접 오버라이드하기보다  
`doGet()`, `doPost()`를 구현합니다.

---

## 3. 기본 클래스 구조

서블릿은 보통 `HttpServlet`을 상속해서 만듭니다.

```java
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class FirstServlet extends HttpServlet {

    @Override
    public void init() throws ServletException {
        System.out.println("init 메서드 호출");
    }

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
        System.out.println("doGet 메서드 호출");
    }

    @Override
    public void destroy() {
        System.out.println("destroy 메서드 호출");
    }
}
```

---

## 4. 서블릿 생명주기

### 1) `init()`
- 서블릿이 처음 메모리에 올라올 때 1번 실행
- 초기 설정 작업 수행

### 2) `doGet()` / `doPost()`
- 클라이언트 요청이 들어올 때마다 실행
- 실제 비즈니스 로직 처리

### 3) `destroy()`
- 서버 종료 또는 서블릿 제거 시 1번 실행
- 마무리 작업 수행

---

## 5. 요청 객체: `HttpServletRequest`

클라이언트가 보낸 요청 정보를 담고 있습니다.

### 자주 쓰는 메서드

| 메서드 | 설명 |
|---|---|
| `getParameter(String name)` | 하나의 파라미터 값 얻기 |
| `getParameterValues(String name)` | 같은 name으로 전달된 여러 값 얻기 |
| `getParameterNames()` | 모든 파라미터 이름 얻기 |
| `getMethod()` | 요청 방식 확인(GET/POST) |
| `getRequestURI()` | 요청 URI 확인 |
| `getHeader(String name)` | 헤더 정보 얻기 |
| `getCookies()` | 쿠키 정보 얻기 |

---

## 6. 폼 데이터 받기

HTML의 `<form>` 태그로 전송된 데이터는 `request` 객체에서 꺼냅니다.

### 예시 HTML

```html
<form action="/test" method="get">
    이름: <input type="text" name="userName">
    <input type="submit" value="전송">
</form>
```

### 예시 Servlet

```java
protected void doGet(HttpServletRequest request, HttpServletResponse response)
        throws ServletException, IOException {

    String userName = request.getParameter("userName");
    System.out.println("이름: " + userName);
}
```

---

## 7. 여러 값 받기

체크박스처럼 같은 이름으로 여러 값이 전달되면 `getParameterValues()`를 사용합니다.

```java
String[] hobbies = request.getParameterValues("hobby");

if (hobbies != null) {
    for (String hobby : hobbies) {
        System.out.println(hobby);
    }
}
```

---

## 8. 응답 객체: `HttpServletResponse`

서버가 클라이언트에게 보내는 응답을 담당합니다.

### 자주 쓰는 기능

| 메서드 | 설명 |
|---|---|
| `setContentType()` | 응답 타입 설정 |
| `setCharacterEncoding()` | 문자 인코딩 설정 |
| `getWriter()` | 문자 기반 출력 |
| `sendRedirect()` | 다른 주소로 이동 |
| `setStatus()` | 상태 코드 설정 |

---

## 9. 응답 보내기 예제

```java
protected void doGet(HttpServletRequest request, HttpServletResponse response)
        throws ServletException, IOException {

    response.setContentType("text/html");
    response.setCharacterEncoding("UTF-8");

    response.getWriter().println("<html>");
    response.getWriter().println("<body>");
    response.getWriter().println("<h1>안녕하세요, 서블릿입니다.</h1>");
    response.getWriter().println("</body>");
    response.getWriter().println("</html>");
}
```

---

## 10. GET과 POST 차이

### GET
- 데이터가 URL에 붙어서 전송됨
- 조회에 많이 사용
- 길이 제한이 있을 수 있음
- 보안에 약함

### POST
- 데이터가 본문(body)에 담겨 전송됨
- 회원가입, 로그인, 저장 처리에 많이 사용
- GET보다 상대적으로 안전

---

## 11. 서블릿 작성 시 꼭 기억할 점

### 1) 클래스 상속
- `HttpServlet` 상속 필수

### 2) 메서드 재정의
- `doGet()` 또는 `doPost()` 오버라이딩

### 3) 예외 처리
- 보통 아래처럼 선언

```java
throws ServletException, IOException
```

### 4) 응답 전에 타입/인코딩 설정
특히 한글 깨짐 방지를 위해 자주 사용

```java
response.setContentType("text/html");
response.setCharacterEncoding("UTF-8");
```

POST 요청에서 한글 파라미터 처리 시에는 보통:

```java
request.setCharacterEncoding("UTF-8");
```

---

## 12. 시험/면접용 핵심 한 줄 정리

### 서블릿이란?
- 웹에서 **요청을 처리하고 응답을 생성하는 자바 클래스**

### 요청 처리 객체
- `HttpServletRequest`

### 응답 처리 객체
- `HttpServletResponse`

### 생명주기
- `init()` → `service()` / `doGet()` / `doPost()` → `destroy()`

### 파라미터 받기
- 한 개: `getParameter()`
- 여러 개: `getParameterValues()`
- 이름들: `getParameterNames()`

---

## 13. 네 필기 코드 오타 정정

### 잘못된 부분 예
- `extend` → `extends`
- `HttpServletResquest` → `HttpServletRequest`
- `pro` → 미완성
- `FirstServlet` 클래스 선언부 수정 필요

### 올바른 형태

```java
public class FirstServlet extends HttpServlet {
    @Override
    public void init() throws ServletException {
        System.out.println("init 메서드 호출");
    }

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

    }
}
```

---

## 14. 진짜 필수만 압축 요약

### 암기 포인트 5개
1. 서블릿은 **요청/응답 처리용 자바 클래스**
2. `HttpServlet`을 상속받아 작성
3. 요청은 `HttpServletRequest`
4. 응답은 `HttpServletResponse`
5. 파라미터는 `getParameter()`로 받음

---

## 15. 초압축 정리

```text
서블릿 = 자바로 만든 웹 요청 처리기
request = 클라이언트가 보낸 데이터
response = 서버가 보내는 결과
doGet() = GET 요청 처리
doPost() = POST 요청 처리
```
