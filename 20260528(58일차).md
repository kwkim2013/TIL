# [자료구조] 58일차 학습 요약: 고급 정렬 II (힙 정렬) & 그래프 최단 경로

## 1. 힙 정렬 (Heap Sort)

> **"완전이진트리로 구현된 우선순위 큐 구조를 이용한 제자리 정렬 알고리즘"**

### ① 핵심 원리 및 배경
* **힙(Heap)의 특징**: 트리 구조 내에서 부모 노드가 자식 노드보다 항상 크거나(최대 힙), 작아야(최소 힙) 하는 계층적 트리 구조입니다. 최댓값 혹은 최솟값을 $O(1)$ 만에 손쉽게 추출할 수 있습니다.
* **제자리 정렬(In-place Sort)**: 일반적인 트리 정렬은 추출된 데이터를 저장하기 위해 별도의 임시 배열(추가 메모리)이 필요하지만, 힙 정렬은 기존 입력 배열 구조를 깨뜨리지 않고 메모리를 내부에서 직접 재활용하여 공간 효율성을 극대화합니다.

### ② 제자리 정렬 매커니즘
1. **Heapify (힙 생성)**: 주어진 일반 리스트 데이터 전체를 아래에서부터 위로 탐색하며 구조적인 **최대 힙(Max Heap)** 상태로 변환합니다.
2. **교환 및 정렬**: 트리의 루트(Root) 노드에 위치한 가장 큰 값을 꺼내어 현재 정렬 대상 배열의 맨 마지막 인덱스 요소와 자리를 맞바꿉니다.
3. **재조정**: 정렬이 확정된 맨 뒤 영역을 제외하고, 남은 앞쪽 트리 데이터 구조를 다시 최대 힙 상태로 재구성(Heapify)합니다.
4. 트리의 모든 요소가 소진될 때까지 2~3번 과정을 반복하여 정렬을 완료합니다.



### ③ 성능 및 파이썬 활용
* **시간 복잡도**: 입력 데이터의 난잡함 정도나 최악, 최상, 평균 분포에 관계없이 항상 **$O(n\log_2n)$**이 정량적으로 일정하게 보장됩니다.
* **적용 장점**: 모든 데이터를 정렬할 필요 없이, 전체 거대한 데이터 더미 중에서 **상위 소수의 레코드(예: Top 10)만 빠르게 필터링**하여 추출해야 하는 요구 사항에서 압도적인 효율을 보여줍니다.
* **파이썬 내장 모듈 (`heapq`)**:
    * `heapq.heapify(list)`: 전달받은 기존 리스트를 최소 힙 구조로 자동 변환합니다.
    * `heapq._heapify_max(list)`: 최대 힙 구조가 핵심인 정렬 논리를 구현하고자 할 때는 언더스코어(`_`)가 포함된 내부 최대 힙 전용 메서드를 응용할 수 있습니다.

---

## 2. 그래프 최단 경로: 플로이드-워셜 알고리즘

> **"모든 정점에서 다른 모든 정점으로 가는 최단 거리를 동적 계획법으로 해결하는 알고리즘"**

### ① 알고리즘 개념 (Dynamic Programming)
네트워크망이나 지도 데이터 등 방향성과 가중치가 존재하는 그래프 구조에서 사용하는 알고리즘입니다. 
특정 정점 $i$에서 정점 $j$로 바로 직행하는 거리 비용과, 임의의 제3의 경유지 정점 $k$를 거쳐서 돌아가는 우회 비용($i \rightarrow k \rightarrow j$)을 끊임없이 전체 비교 및 메모이제이션 테이블에 기록하며 최적의 비용 값으로 전체 행렬을 갱신해 나갑니다.

### ② 수식적 접근 및 조건 (Relaxation)
$$\text{if } (A[i][k] + A[k][j] < A[i][j]): A[i][j] = A[i][k] + A[k][j]$$
* **해석**: "기존에 다이렉트로 가던 비용(`A[i][j]`)보다 정점 $k$를 찍고 우회해서 도달하는 경로 가중치의 합(`A[i][k] + A[k][j]`)이 더 낮고 유리하다면, 기존 테이블의 인덱스 값을 더 저렴한 우회 경로의 값으로 교체한다."

### ③ 알고리즘 구조 (3중 루프 구성)
그래프의 총 정점 개수를 `vsize`라고 할 때, 반드시 **경유지($k$) -> 출발지($i$) -> 도착지($j$)**의 순서 규칙성을 갖는 3중 중첩 `for` 루프 구조로 설계됩니다.

```python
# 플로이드 최단 경로 파이썬 예시 코드
def floyd_warshall(vsize, adj):
    # 인접 행렬 테이블 복사본 생성 및 초기화
    A = [list(row) for row in adj]
    
    # 3중 Nested Loop
    for k in range(vsize):      # Outer: 1단계 경유지 정점 설정
        for i in range(vsize):  # Mid:   2단계 출발 정점 설정
            for j in range(vsize): # Inner: 3단계 도착 정점 설정
                # 경유지를 거치는 경로가 더 짧은 경우 거리 배열 갱신
                if (A[i][k] + A[k][j] < A[i][j]):
                    A[i][j] = A[i][k] + A[k][j]
    return A