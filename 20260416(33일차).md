## 📅 33일차 (04/16 수) - C++ , Python, OpenCV , linux

### 📌 학습 주제

- 기초 문법 이해
- → 포인터 개념 학습
- → 파일 입출력(EOF) 처리
- → VSCode 실행 환경 문제 해결
- → 인코딩(BOM) 문제 디버깅
- → 실무형 문제 해결 경험 축적

---

### 📚 상세 학습 내용


#### 1️⃣ C++ 기본 개념 핵심

### std:: 의미

```c++
std::cout
std::cin
```
```c++
std = standard (표준 라이브러리)
::  = 범위 지정 연산자
```

```c++
std::cout
→ 표준 라이브러리에 있는 출력 객체
```

### cin 사용 시점

```c++
cin >> num;
```
```
사용자로부터 입력을 받을 때 사용
```

### << 와 >> 의미

```c++
cout << data;
cin >> data;
```

```c++
<<  = 출력 연산자 (보내기)
>>  = 입력 연산자 (받기)

```

#### 2️⃣ 포인터 핵심 요약
### 기본 구조

```c++
int n = 10;
int* p = &n;

```
의미
```c++
&  = 주소
*  = 포인터
p  = n의 주소를 저장하는 변수
```

### 변수 이름에 p 붙이는 이유

```c++
int* pn1;

p는 규칙이 아니라 관례 (pointer 의미)

즉 pn1 → n1의 포인터
```


### 포인터 핵심 4개 키워드
```c++
&  주소 연산자
*  역참조 연산자
NULL / nullptr
포인터 변수

```

#### 3️⃣ 파일 입출력 핵심 (EOF 패턴)

```c++
ifstream infile("numbers.dat");

while(infile >> num)
{
    sum += num;
}

```

#### EOF 반복문 핵심
파일에서 읽기 성공 → 반복
읽기 실패(EOF) → 종료

#### 4️⃣ 이번 문제의 핵심 원인 (중요)

### 에러 원인?
```
합 = 0 출력
원인
UTF-8 BOM (Byte Order Mark)가 저장되어 있어서 읽을 수가 없었다.

파일 맨 앞에 EF BB BF 라는 숨은 문자가 있어서 while(infile >> num)

첫 숫자 읽기 실패 → 반복문 0번 실행 → sum = 0

해결 방법
방법 1 (가장 추천)
파일을 UTF-8 (BOM 없음) 으로 저장


메모장에서 하단을 보면 UTF-8 (BOM)으로 저장하게 되면 읽을 수 없게 됨
Save with Encoding
→ UTF-8
이 문제에서 얻은 중요한 경험 경로 문제 아님
문법 문제 아님 인코딩 문제였음

```



---

### 💻 학습 성과 
* 파일 입출력 구조 이해
* EOF 반복문 패턴 이해
* 포인터 기본 개념 이해
* VSCode 실행 구조 이해* 경로 문제 해결 경험
* 인코딩(BOM) 문제 해결 경험
* 디버깅 능력 향상

---



