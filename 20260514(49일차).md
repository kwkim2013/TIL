# 📅 2026-05-14 - 자료구조 (파이썬) 

# 이진 트리 응용 : 모르스 코드 결정트리 정리

## 1. 이진트리 구현 예제

```python

# 이진 트리의 노드 클래스를 정의
class TNode:
    # 생성자: 노드를 만들 때 데이터와 왼쪽/오른쪽 자식 노드를 저장
    def __init__(self, data, left, right):
        self.data = data      # 현재 노드의 데이터
        self.left = left      # 왼쪽 자식 노드
        self.right = right    # 오른쪽 자식 노드


# 중위 순회 함수
# 순서: 왼쪽 자식 -> 현재 노드 -> 오른쪽 자식
def inorder(n):
    if n is not None:          # 노드가 존재하면
        inorder(n.left)        # 왼쪽 서브트리 방문
        print(n.data, end=' ') # 현재 노드 출력
        inorder(n.right)       # 오른쪽 서브트리 방문


# 전위 순회 함수
# 순서: 현재 노드 -> 왼쪽 자식 -> 오른쪽 자식
def preorder(n):
    if n is not None:
        print(n.data, end=' ') # 현재 노드 출력
        preorder(n.left)       # 왼쪽 서브트리 방문
        preorder(n.right)      # 오른쪽 서브트리 방문


# 후위 순회 함수
# 순서: 왼쪽 자식 -> 오른쪽 자식 -> 현재 노드
def postorder(n):
    if n is not None:
        postorder(n.left)      # 왼쪽 서브트리 방문
        postorder(n.right)     # 오른쪽 서브트리 방문
        print(n.data, end=' ') # 현재 노드 출력


# 레벨 순회 함수
# 위에서 아래로, 왼쪽에서 오른쪽으로 방문
def levelorder(root):
    queue = []                # 큐(리스트 사용)
    queue.append(root)        # 시작 노드(root)를 큐에 삽입

    while queue:              # 큐가 빌 때까지 반복
        n = queue.pop(0)      # 큐의 맨 앞 노드를 꺼냄
        if n is not None:     
            print(n.data, end=' ') # 현재 노드 출력
            queue.append(n.left)   # 왼쪽 자식을 큐에 삽입
            queue.append(n.right)  # 오른쪽 자식을 큐에 삽입


# 전체 노드 개수를 세는 함수
def count_node(n):
    if n is None:             # 노드가 없으면
        return 0
    return 1 + count_node(n.left) + count_node(n.right)
    # 현재 노드 1개 + 왼쪽 서브트리 노드 수 + 오른쪽 서브트리 노드 수


# 단말(리프) 노드 개수를 세는 함수
# 단말 노드 = 자식이 하나도 없는 노드
def count_leaf(n):
    if n is None:
        return 0
    if n.left is None and n.right is None:  # 왼쪽, 오른쪽 자식이 모두 없으면 단말 노드
        return 1
    return count_leaf(n.left) + count_leaf(n.right)
    # 왼쪽/오른쪽 서브트리의 단말 노드 개수를 합함


# 트리의 높이를 계산하는 함수
def calc_height(n):
    if n is None:             # 노드가 없으면 높이는 0
        return 0
    hLeft = calc_height(n.left)    # 왼쪽 서브트리 높이
    hRight = calc_height(n.right)  # 오른쪽 서브트리 높이
    return max(hLeft, hRight) + 1  # 더 큰 높이 + 현재 노드 1


# ===== 트리 구성 =====
# D, E, F는 리프 노드(자식 없음)
d = TNode('D', None, None)
e = TNode('E', None, None)
f = TNode('F', None, None)

# B는 왼쪽 자식 D, 오른쪽 자식 E를 가짐
b = TNode('B', d, e)

# C는 왼쪽 자식 F, 오른쪽 자식 없음
c = TNode('C', f, None)

# A는 루트 노드, 왼쪽 자식 B, 오른쪽 자식 C
root = TNode('A', b, c)


# ===== 순회 결과 출력 =====
print('\n   In-Order : ', end='')
inorder(root)

print('\n  Pre-Order : ', end='')
preorder(root)

print('\n Post-Order : ', end='')
postorder(root)

print('\nLevel-Order : ', end='')
levelorder(root)

print()


# ===== 기타 정보 출력 =====
print(" 노드의 개수 = %d개" % count_node(root))   # 전체 노드 수
print(" 단말의 개수 = %d개" % count_leaf(root))   # 리프 노드 수
print(" 트리의 높이 = %d" % calc_height(root))    # 트리 높이


```

---

## 2. 탐색트리

탐색을 위한 트리 기반의 자료구조

기존 탐색 방법의 한계
- 순차 탐색 : 간단하지만, 비효율적
- 이진 탐색 : 정렬된 데이터에 대해서만 적용
  - 삽입 또는 삭제의 빈도가 높다면, 이때마다 정렬이 필요함
- 해싱 : 메모리 사용량, 해시 충돌, 오버플로 


### 2.1 이진 탐색 트리(BST, Binary Search Tree)

효율적인 탐색을 위한 이진트리 기반의 자료구조 : O(log2n)

### 2.2  이진 탐색 트리 연산 : 노드

노드의 데이터는 하나의 엔트리 형태이여야 함
- 탐색키
- 키에 대한 값

### 2.3 이진 탐색 트리 연산 : 탐색

값을 이용한 탐색 O(n)
- 트리의 모든 노드를 검사해야 함
- 순회 방법, 데이터 특성에 따라서 좋은 결과가 있을 수도 있지만, 
  일반적으로 비효율적임 시작 노드가 왼쪽으로 가는 특성이 있음

### 2.4 이진 탐색 트리 연산 : 탐색

이진 탐색 트리는 다음 규칙을 가진다.
- 키를 이용한 탐색 O(log₂n)
- 키 비교 상태
  - key == 루트의 값 : 로트가 찾는 로드임 (탐색 성공)
  - key < 루트의 키 값 : 찾는 노드가 왼쪽 서브트리에 있음
  (루트의 왼쪽 자식을 기준으로 다시 탐색 시작)
  - key > 루트의 키 값 : 찾는 노드가 왼쪽 서브트리에 있음
  (루트의 오른쪽 자식을 기준으로 다시 탐색 시작)

### 2.5 이진 탐색 트리 연산 : 탐색

최대 키 노드 찾기
- 이진탐색트리에 존재하는 최대 키를 가지는 노드
최소 키 노드 찾기
- 이진탐색트리에 존재하는 최소 키를 가지는 노드


### 2.6 이진 탐색 트리 연산 : 삽입

탐색 과정을 수행하면서, 탐색에 실패한 위치에 새로운 노드 삽입
- 만약 동일한 키가 존재한다면, 삽입하지 않음(키의 중복 허용 않을 시에)


### 2.7 이진 탐색 트리 연산 : 삭제

Case3: 비단말 노드 (두개의 자식)
- 가장 비슷한 값을 가진 노드를 삭제 위치로 가져옴 (후계 노드 선택)
- 이진 탐색 트리 특징을 유지해야 함(크기, 형태)
- 후계 노드의 자식 수 고려해야함

```text

          [35]
         /    \
      [(18)]   [68]
      /    \      \
    [7]    [26]   [99]
   /  \    /  \
 [3] [12][22] [30]
      ^    ^
      |____|____ 삭제될 '18'의 자리를 대체할 수 있는 후보들

```
---

```python

def delete_bst_case3 (parent, node, root) :
  succp = node
  succ = node.right
  while (succ.left != None) :
    succp = succ
    succ = succ.left

  if (succp.left == succ) :
    succp.left = succ.right
  else :
    succp.right = succ.right

  node.key = succ.key
  node.value= succ.value
  node = succ;

  return root

```

### 2.7 이진 탐색 트리 연산 : 삭제

Case 1~3

```python
def delete_bst (root, key) :
    if root == None : return None
    
    parent = None
    node = root
    while node != None and node.key != key :
        parent = node
        if key < node.key : node = node.left
        else : node = node.right

    if node == None : return None
    if node.left == None and node.right == None:
        root = delete_bst_case1 (parent, node, root)
    elif node.left==None or node.right==None :
        root = delete_bst_case2 (parent, node, root)
    else :
        root = delete_bst_case3 (parent, node, root)

```

---

### 2.8 이진 탐색 트리 성능

트리의 형태에 따라서 성능이 달라짐
탐색, 삽입, 삭제 연산의 시간은 트리 높이에 비례



| 연산 | 함수 | 최선의 경우 (균형트리) | 최악의 경우 (경사트리) |
| :--- | :--- | :---: | :---: |
| 키를 이용한 탐색 | `search_bst()`, `search_bst_iter()` | $O(\log_2 n)$ | $O(n)$ |
| 값을 이용한 탐색 | `search_value_bst()` | $O(n)$ | $O(n)$ |
| 최대/최소 노드 탐색 | `search_max_bst()`, `search_min_bst()` | $O(\log_2 n)$ | $O(n)$ |
| 삽입 | `insert_bst()` | $O(\log_2 n)$ | $O(n)$ |
| 삭제 | `delete_bst()` | $O(\log_2 n)$ | $O(n)$ |


---

### **분석 요약**
1. **균형 이진 트리 (Best Case):** 트리의 높이가 $\log_2 n$에 가깝게 유지될 때, 탐색, 삽입, 삭제 연산은 매우 효율적인 $O(\log_2 n)$의 성능을 보입니다.
2. **경사 이진 트리 (Worst Case):** 트리가 한쪽으로 치우쳐 연결 리스트와 같은 형태가 되면, 높이가 $n$이 되어 모든 연산의 복잡도가 $O(n)$으로 증가합니다.
3. **값 기반 탐색:** 키(Key)가 아닌 값(Value)으로 탐색할 경우, 이진 탐색 트리의 속성을 활용할 수 없으므로 모든 노드를 확인해야 하는 $O(n)$의 시간이 소요됩니다.

### 2.9 이진 탐색트리 기반 맵

이진탐색트리를 이용한 맵 구현

정의 7.1 Map ADT
데이터 : 키를 가진 레코드(엔트리)의 집합
연산
- search(key): 탐색 key를 가진 레코드를 찾아 반환한다.
- insert(entry): 주어진 entry를 맵에 삽입한다.
- delete(key): 탐색키 key를 가진 레코드를 찾아 삭제한다.

```python
class BSTMap():
  def __init__ (self):
      self.root = None

  def isEmpty (self): return self.root == None
  def clear(self): self.root = None
  def size(self): return count_node(self.root)

  def search(self, key): return search_bst(self.root, key)
  def searchValue(self, key): return search_value_bst(self.root, key)
  def findMax(self): return search_max_bst(self.root)
  def findMin(self): return search_min_bst(self.root)

  def insert(self, key, value=None):
    n = BSTNode(key, value)
    if self.isEmpty() :
       self.root = n
    else :
      insert_bst(self.root, n)

  def delete(self, key):
      delete_bst (self.root, key)

  def display(self, msg = 'BSTMap :'):
      print(msg, end='')
      inorder(self.root)
      print()
```

---



## 3. 균형이진탐색트리 (AVL tree, Adelson - Velskii, Landis, 1962년)

균형있는 트리를 보장
- 탐색, 삽입, 삭제: O(log₂n)

  AVL 트리
  - AVL 트리는 모든 노드에서 왼쪽 서브트리의 높이 차가 1을 넘지 않은 이진탐색
    트리이다. 즉 모든 노드의 균형 인수는 0이나 ±1이 되어야 한다.

균형인수(balance factor)
- 왼쪽 서브트리의 높이와 오른쪽 서브트리의 높이 차

### 3.1 균형이진탐색트리 연산 : 삽입

○ 삽입이 발생하면, 해당 노드에서 루트까지의 경로에 있는 조상 노드들의 균형 인수가 영향을 받음
○ 삽입 후에, 불균형 상태로 변한 가장 가까운 조상 노드의 서브 트리들에 대하여 다시 재균형 과정 수행
  - 삽입 노드부터 균형 인수가 ±2가 된 가장 가까운 조사 노드까지 회전
○ 균형 깨지는 경우
  - LL, LR, RR, RL
○ 회전 방식
  - 단순 회전(single rotation): 한번 회전
  - 이중 회전( double rotation): 두번 회전

  ### 3.2 균형이진 탐색트리 연삽 : 삽입

  재균형 함수

```python
def reBalance (parent) :
    hDiff = calc_height_diff(parent)

  if hDiff > 1 :
      if calc_height_diff( parent.left ) > 0 :
        parent = rotateLL( parent )
    else :
        parent = rotateLR( parent )
  elif hDiff < -1 :
      if calc_height_diff( parent.right ) < 0 :
        parent = rotateRR( parent )
  else :
        parent = rotateRL( parent )
  return parent

def calc_height(n) :
  if n is None :

    return 0
  hLeft = calc_height(n.left)
  hRight = calc_height(n.right)
  if (hLeft > hRight) :
    return hLeft + 1
else:
    return hRight + 1

def calc_height_diff(n) :
  if n==None :
    return 0
  return calc_height(n.left) -
calc_height(n.right)
    

```

### 3.2 균형 이진탐색트리 이용한 맵

맵구현


## 4. 그래프 정의
G = (V,E)

  -
## 4.1 그래프 종류

가중치 그래프(weight graph) / 네트워크(network)
- 간선에 비용이나 가중치가 할당된 그래프
- 정점의 연결 유무, 연결 강도 표현

부분 그래프(subgraph)
- 그래프의 부분 집합으로 이루어진 그래프
- 정점, 간선을 일부분만 표현

## 4.2 그래프 용어

인접 정점(adjacent vertex)
  - 간선에 의해 직접 연결된 정점
 예시)
  - A의 인접 정점: B, C, D
  - B의 인접 정점 : A, C

정점의 차수(degree)
- 정점에 연결된 간선의 수
- 무 방향 그래프
  - 정점에 인접한 정점의 수
  - 차수의 합 = 간선 수의 2배

- 방향 그래프
  - 진입차수(in-degree): 외부에서 정점으로 오는 간선의 수
  - 진출차수(out-degree): 정점에서 외부로 향하는 간선의 수
  - 진입/진출 차수의 합 = 간선의 수


## 4.3 그래프 용어

- 경로(path)
  - 간선을 따라 갈수 있는 길이며 정점의 나열로 표시
  - 경로의 길이: 경로를 구성하는데 사용된 간선의 수
- 단순 경로(simple path)
  - 경로 중에서 반복되는 간선이 없는 경로
  - 예시)
    - B,A,C,D or D,C,A,B 등
    - A,D,C,B or A,B,C,D 등
  - 예외) B,A,C,A 등
- 사이클(cycle)
  - 시작 정점과 종료 정점이 같은 단순 경로
  - 예시)
    - A,B,C,A or B,C,A,B or C,A,B,C 등
    - A,B,A or B,A,B

