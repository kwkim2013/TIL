# 📅 2026-05-08 - javascript
## 개요

### 1. 자바스크립트?

- 프로그래밍 : 컴퓨터가 동작할 수 있게 컴퓨터 언어로 명령어를 나열하는 행위
- Front-end : HTML 구조(뼈대) , CSS 디자인(꾸미기), JavaScript 웹페이지 동작 (기능)
- back-end : java 서버 로직처리 , python 데이터 처리(AI), PHP 서버스크립트, Node.js js 기반 서버

### 2. 입출력 방식
- 입력 -> 처리 -> 출력
- 입력 console(키보드), 마우스, 마이크, 카메라 센서, 온습도 센서
- 처리 (CPU, RAM)
- 출력 console(모니터) 프린터, 액츄에이터(모터)

Frontend (화면)          Backend (서버)
┌─────────────┐         ┌─────────────┐
│   HTML      │         │   DB 연동   │
│   CSS       │  ←→     │   로그인    │
│ JavaScript  │         │  데이터처리 │
└─────────────┘         └─────────────┘
  사용자가 보는 부분        보이지 않는 부분


### 1. 대화상자(dialog)

```javascript
// ✅ alert - 단순 알림창
alert("안녕하세요!");

// ✅ confirm - 확인/취소 선택창 (true/false 반환)
let result = confirm("정말 삭제하시겠습니까?");
if (result) {
    console.log("확인 클릭");
} else {
    console.log("취소 클릭");
}

// ✅ prompt - 입력창 (입력값 반환)
let name = prompt("이름을 입력하세요:");
console.log("입력값: " + name);
```

---

### 2. 문서 출력
```javascript
// ✅ document.write - HTML 문서에 직접 출력
document.write("<h1>안녕하세요</h1>");
document.write("현재 시간: " + new Date());

// ✅ console.log - 개발자 도구 콘솔에 출력 (디버깅용)
console.log("콘솔 출력");


```

### 3.External(외부 JS 파일 연결)

```javascript
<!-- HTML에서 외부 JS 파일 불러오기 -->
<script src="파일명.js"></script>

<!-- ✅ 예시 -->
<head>
    <script src="script.js"></script>  <!-- head에 연결 -->
</head>
<body>
    <script src="script.js"></script>  <!-- body 끝에 연결 (권장) -->
</body>

```

### 4.DOM 선택 & 이벤트

```javascript
// ✅ querySelector - CSS 선택자로 요소 선택
const heading = document.querySelector("#heading"); // ID 선택
const para = document.querySelector(".text");       // Class 선택
const tag = document.querySelector("h1");           // 태그 선택

// ✅ onclick 이벤트 - 클릭시 실행
heading.onclick = function() {
    heading.style.color = "red";  // 클릭하면 글자색 빨간색
}

```

### 5. const vs let vs var

```javascript
// ✅ const - 상수 (재선언 ❌, 재할당 ❌)
const PI = 3.14;
// PI = 3.15;  ← 오류 발생!

// ✅ let - 변수 (재선언 ❌, 재할당 ✅)
let radius = 5;
radius = 10;    // 가능 ✅
// let radius = 20; ← 오류 발생!

// ✅ var - 구식 변수 (재선언 ✅, 재할당 ✅) → 사용 비권장
var sum = 10;
var sum = 20;   // 재선언 가능 (오류 없음) ← 버그 위험!

```
#### 비교표

| 구분 | var | let | const |
|------|-----|-----|-------|
| 재선언 | ✅ 가능 | ❌ 불가 | ❌ 불가 |
| 재할당 | ✅ 가능 | ✅ 가능 | ❌ 불가 |
| 권장여부 | ❌ 비권장 | ✅ 권장 | ✅ 권장 |

### 6. prompt + 계산 예제

```javascript
const PI = 3.14;
let radius = prompt("반지름을 입력하세요: ");

// ⚠️ prompt는 문자열 반환 → 숫자 변환 필요!
let area = PI * Number(radius) * Number(radius);

console.log("반지름이 " + radius + "인 원의 넓이는 " + area + "입니다.");

```

### 7. 함수 (function)

```javascript
// ✅ 기본 함수 선언
function add(a, b) {
    return a + b;
}
let sum = add(10, 20);
console.log(sum); // 30

// ✅ Math 내장 함수
Math.floor(3.9);      // 3  (내림)
Math.random();        // 0~1 사이 랜덤값
Math.floor(Math.random() * 255); // 0~254 랜덤 정수

```

### 8. 랜댐 배경색 예제(종합)

```javascript
// ✅ 랜덤 숫자 생성 함수
function random(number) {
    return Math.floor(Math.random() * number);
}

// ✅ 배경색 변경 함수
function bgChange() {
    const rndCol = 'rgb(' + random(255) + ',' 
                          + random(255) + ',' 
                          + random(255) + ')';
    document.body.style.backgroundColor = rndCol;
}

bgChange(); // 함수 호출


```

#### 전체 흐름 요약

사용자 입력  →  prompt / confirm / alert
      ↓
데이터 처리  →  변수(let, const), 함수(function)
      ↓
화면 출력    →  document.write / console.log
      ↓
DOM 조작     →  querySelector + style 변경

💡 핵심 팁: var 대신 let/const 사용 습관화, prompt 입력값은 항상 Number() 변환 후 계산!