# 📅 2026-05-12 - Java 웹 프로그래밍

## 개요

이번 학습에서는 **서블릿(Servlet)** 의 개념과 역할, **Servlet API의 계층 구조**, **서블릿 생명 주기 메서드**, **FirstServlet 실습**, **서블릿 동작 과정**, 그리고 **애너테이션을 이용한 서블릿 매핑**에 대해 정리한다.

---

## 1. 서블릿 이해하기

### 1) 서블릿이란?

서블릿(Servlet)은 **자바 기반의 웹 프로그래밍 기술**로, 웹 브라우저의 요청(Request)을 받아 처리하고 동적으로 서비스를 제공한다. 그 결과를 웹 브라우저에 응답(Response)하는 **자바 클래스**이다.

즉, 서블릿은 웹 서버와 자바 프로그램을 연결해 주는 역할을 한다.

### 2) 서블릿의 특징

- 자바 언어를 기반으로 동작한다.
- 웹 서버에서 실행된다.
- 클라이언트의 요청을 처리하고 응답을 생성한다.
- HTML을 동적으로 생성할 수 있다.
- 재사용성과 유지보수성이 높다.

### 3) 서블릿이 필요한 이유

기존의 정적인 HTML만으로는 사용자 요청에 따라 다른 결과를 보여주기 어렵다.  
서블릿을 사용하면 다음과 같은 작업이 가능하다.

- 로그인 처리
- 회원가입 처리
- 게시글 조회
- 데이터베이스 연동
- 동적 페이지 생성

---

## 2. 서블릿 API 계층 구조와 기능

서블릿은 **Servlet API**를 기반으로 동작한다. 주요 계층 구조는 다음과 같다.

### 1) 계층 구조

```text
Servlet
 └── GenericServlet
      └── HttpServlet
```

### 2) 주요 인터페이스와 클래스

#### (1) Servlet 인터페이스
서블릿의 기본 규칙을 정의한 최상위 인터페이스이다.

주요 메서드:
- `init()`
- `service()`
- `destroy()`
- `getServletConfig()`
- `getServletInfo()`

#### (2) GenericServlet 클래스
`Servlet` 인터페이스를 구현한 **추상 클래스**이다.  
서블릿 개발 시 공통 기능을 일부 구현해 놓았기 때문에 개발자가 더 쉽게 사용할 수 있다.

특징:
- 프로토콜에 독립적이다.
- `service()` 메서드는 직접 구현해야 한다.

#### (3) HttpServlet 클래스
웹 프로그래밍에서 가장 많이 사용하는 클래스이다.  
HTTP 요청 방식을 처리하기 위해 만들어졌다.

주요 메서드:
- `doGet()`
- `doPost()`
- `doPut()`
- `doDelete()`

즉, 웹 애플리케이션에서는 보통 `HttpServlet`을 상속받아 서블릿을 작성한다.

---

## 3. 서블릿 생명 주기 메서드

서블릿은 생성부터 소멸까지 일정한 생명 주기를 가진다.

### 1) 생명 주기 순서

1. 서블릿 객체 생성
2. `init()` 호출
3. `service()` 호출
4. `destroy()` 호출

### 2) 주요 메서드 설명

#### (1) `init()`
- 서블릿이 처음 생성될 때 **한 번만 호출**된다.
- 초기화 작업을 수행한다.
- 예: DB 연결 준비, 설정값 로딩

#### (2) `service()`
- 클라이언트 요청이 들어올 때마다 호출된다.
- 실제 요청을 처리하는 핵심 메서드이다.
- `HttpServlet`에서는 요청 방식에 따라 `doGet()`, `doPost()` 등을 자동으로 분기한다.

#### (3) `destroy()`
- 서블릿이 종료될 때 **한 번만 호출**된다.
- 자원 해제 작업을 수행한다.
- 예: 연결 종료, 메모리 정리

### 3) 정리

- **초기화**: `init()`
- **요청 처리**: `service()` / `doGet()` / `doPost()`
- **종료 처리**: `destroy()`

---

## 4. FirstServlet을 이용한 실습

### 1) 기본 서블릿 예제

```java
import java.io.IOException;
import java.io.PrintWriter;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

@WebServlet("/first")
public class FirstServlet extends HttpServlet {

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        response.setContentType("text/html; charset=UTF-8");
        PrintWriter out = response.getWriter();

        out.println("<html>");
        out.println("<head><title>FirstServlet</title></head>");
        out.println("<body>");
        out.println("<h1>첫 번째 서블릿 실행</h1>");
        out.println("</body>");
        out.println("</html>");
    }
}
```

### 2) 코드 설명

- `HttpServlet`을 상속받아 서블릿을 만든다.
- `@WebServlet("/first")`를 사용해 URL을 매핑한다.
- 브라우저에서 `/first` 요청이 들어오면 `doGet()`이 실행된다.
- `response.getWriter()`를 통해 HTML 응답을 작성한다.

### 3) 실행 결과

브라우저에서 다음과 같이 요청하면:

```text
http://localhost:8080/프로젝트명/first
```

화면에 다음 메시지가 출력된다.

```text
첫 번째 서블릿 실행
```

---

## 5. 서블릿 동작과정

서블릿이 동작하는 전체 흐름은 다음과 같다.

### 1) 처리 순서

1. 사용자가 브라우저에서 URL 요청
2. 웹 서버 / WAS가 요청 수신
3. 요청 URL과 매핑된 서블릿 확인
4. 서블릿 객체가 없으면 생성 후 `init()` 호출
5. `service()` 또는 `doGet()`, `doPost()` 실행
6. 처리 결과를 응답 객체에 저장
7. 웹 브라우저에 응답 전송

### 2) 흐름 설명

#### 사용자 요청
사용자가 주소창에 URL을 입력하거나 링크를 클릭한다.

#### WAS 요청 분석
톰캣(Tomcat) 같은 WAS가 요청을 받고 어떤 서블릿이 처리할지 확인한다.

#### 서블릿 실행
해당 서블릿이 메모리에 없으면 객체를 생성하고 초기화한 뒤 요청을 처리한다.

#### 응답 반환
서블릿이 HTML, JSON, 문자열 등의 결과를 만들어 브라우저에 전달한다.

---

## 6. 애너테이션을 이용한 서블릿 매핑

과거에는 `web.xml` 파일에 서블릿을 등록했지만, 최근에는 **애너테이션 방식**을 많이 사용한다.

### 1) 기본 형식

```java
@WebServlet("/mapping")
public class MappingServlet extends HttpServlet {
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
        response.getWriter().println("매핑 성공");
    }
}
```

### 2) 설명

- `@WebServlet("/mapping")`
  - 브라우저 요청 URL과 서블릿 클래스를 연결한다.
- `/mapping` 요청이 들어오면 해당 서블릿이 실행된다.

### 3) 장점

- `web.xml` 설정 없이 간단하게 등록 가능
- 가독성이 좋다
- 유지보수가 편리하다

### 4) 여러 URL 매핑

```java
@WebServlet(urlPatterns = {"/a", "/b", "/c"})
public class MultiMappingServlet extends HttpServlet {
}
```

위와 같이 하나의 서블릿에 여러 URL을 연결할 수도 있다.

---

## 핵심 요약

### 서블릿
- 자바로 작성하는 웹 컴포넌트
- 요청을 처리하고 응답을 생성함

### Servlet API 계층 구조
- `Servlet` → `GenericServlet` → `HttpServlet`

### 생명 주기
- `init()` : 초기화
- `service()` : 요청 처리
-  `doGet()` : 서블릿 요청시 매번 호출됩니다.
-  `doPost()` : 실제로 클라이언트가 요청하는 작업을 수행합니다.
- `destroy()` : 서블릿이 기능을 수행하고 메모리에서 소멸될 때 호출됩니다. 
                서블릿의 마무리 작업을 주로 수행합니다.

### FirstServlet 실습
- `HttpServlet` 상속
- `@WebServlet`으로 URL 매핑
- `doGet()`에서 응답 생성

### 동작 과정
- 브라우저 요청 → WAS 수신 → 서블릿 실행 → 응답 반환

### 애너테이션 매핑
- `@WebServlet` 사용
- 간단하고 직관적인 URL 연결 가능

---

## 한 줄 정리

**서블릿은 자바 기반 웹 프로그래밍의 핵심 기술로, 클라이언트 요청을 처리하고 동적인 웹 응답을 생성하는 서버 측 자바 클래스이다.**
