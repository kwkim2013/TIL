## 📅 36일차 (04/21 화) - java

### 📌 학습 주제

- 
- 
- 
- 
- 
- 

---

### 📚 상세 학습 내용



#### 📌 상속(1) 핵심 정리 (Java)
### 1. 상속 (Inheritance)
✔ 개념
- 부모 클래스의 필드와 메소드를 자식 클래스가 물려받는 것
- 코드 재사용 가능
- 클래스 구조가 간결해짐

👉 부모 → 슈퍼 클래스 (Super Class)
👉 자식 → 서브 클래스 (Sub Class)


```java
class Person { }

class Student extends Person { }
```
📖 핵심 포인트

- extends 키워드 사용
- 자식 클래스는 부모의 기능을 그대로 사용 가능
- 새로운 기능 추가 가능
상속을 사용하면 중복 코드가 줄어들고 클래스 관리가 쉬워진다


### 2. 상속의 장점

- 코드 재사용 가능
- 유지보수 쉬움
- 생산성 향상
- 클래스 구조를 계층적으로 관리 가능

```
Person
 ├── Student
 ├── Professor
 └── Researcher
```

### 3. 자바 상속 특징 (시험 자주 나옴 ⭐)

| 항목 | 내용 |
|------|------|
| 다중 상속 | ❌ 지원 안함 |
| 상속 횟수 | 제한 없음 |
| 최상위 클래스 | `Object` |
| 모든 클래스 | 자동으로 `Object` 상속 |

```java
class A { }  
// 실제로는
class A extends Object { }
```
모든 클래스는 자동으로 java.lang.Object를 상속받는다

#### 4. 접근 지정자 (상속에서 중요 ⭐)

| 접근제어자 | 같은 클래스 | 같은 패키지 | 자식 클래스 | 다른 패키지 |
|------------|-------------|-------------|-------------|-------------|
| public     | O           | O           | O           | O           |
| protected  | O           | O           | O           | X           |
| default    | O           | O           | X           | X           |
| private    | O           | X           | X           | X           |

```
private → 상속받아도 접근 불가
protected → 자식 클래스 접근 가능
```

```java
class Person {
    private int weight;
}

class Student extends Person {
    void test() {
        // weight 사용 불가
    }
}
```
- private 멤버는 클래스 내부에서만 접근 가능하다

#### 5. 생성자 실행 순서 (매우 중요 ⭐)

객체 생성 시
```java
new Student();
```
1. 부모 생성자 실행
2. 자식 생성자 실행

- 서브 클래스 객체 생성 시 슈퍼 클래스 생성자가 먼저 실행된다


#### 6. super 키워드
1. 부모 생성자 호출

```java
super();
super(x, y);
```

생성자의 첫 줄에 와야 함

```java
class A {
    A(int x) { }
}

class B extends A {
    B() {
        super(10);
    }
}

```

#### 7. 업캐스팅 (Upcasting) ⭐⭐⭐

개념
자식 → 부모 타입으로 변환

```java
Student s = new Student();

Person p = s;

```

특징

- 자동 변환
- 안전함
- 부모 기능만 사용 가능

```java
p.name   → 가능
p.grade  → 불가능
```

서브 클래스 객체는 슈퍼 클래스 타입으로 취급할 수 있다

- super()는 슈퍼 클래스 생성자를 명시적으로 호출한다

#### 8. 다운캐스팅 (Downcasting) ⭐⭐⭐

부모 → 자식 타입으로 변환한다.

```java

Person p = new Student();

Student s = (Student)p;
```

#### 9. instanceof (타입 확인)

다운캐스팅 전에 사용
```java
if (p instanceof Student) {
    Student s = (Student)p;
}

```
```
true / false
```

#### 10. 메소드 오버라이딩 (Overriding) ⭐⭐⭐
부모 메소드를 자식에서 다시 정의

조건은 이름 동일, 매개변수 동일, 반환형 동일 해야한다.

### 📌 예시
```java
class Shape {
    void draw() {
        System.out.println("Shape");
    }
}

class Circle extends Shape {
    void draw() {
        System.out.println("Circle");
    }
}

```

#### 11. 동적 바인딩 (Dynamic Binding)
실행할 메소드를 실행 시 결정

```java
Shape s = new Circle();

s.draw();
```
결과 :
```
Circle
```
이유:
```
자식 메소드가 실행됨
```




### 12. 추상클래스 (Abstract Class)란?
### 📌 한 줄 정의
객체를 만들 수 없는 클래스이며, 추상메서드를 포함할 수 있는 클래스

```java
abstract class Animal {

    String name;   // 변수

    void eat() {   // 일반 메서드
        System.out.println("먹는다");
    }

    abstract void sound();   // 추상 메서드
}
```
#### 📌 13. 인터페이스(Interface)
개념 : 메소드 규격 (틀)

```java
interface Phone {
    void call();
}
```

사용:
```java
class Samsung implements Phone {
    public void call() {
        System.out.println("전화");
    }
}
```
| 항목       | 내용                 |
|------------|----------------------|
| 객체 생성  | 불가                 |
| 다중 상속  | 가능                 |
| 메소드     | 반드시 구현해야 함   |

---
### 💡 느낀 점

- 상속(Inheritance)의 개념과 구조를 이해하고, extends 키워드를 사용하여 부모 클래스의 기능을 자식 클래스에서 재사용하는 방법을 학습하였다.
- 접근제어자(public, protected, default, private)에 따라 상속 시 접근 가능한 범위가 달라지는 것을 이해하였다.
- 객체 생성 시 부모 생성자가 먼저 실행되고 자식 생성자가 실행되는 생성자 호출 순서를 이해하였다.
- super() 키워드를 사용하여 부모 생성자를 명시적으로 호출하는 방법을 학습하였다.
- **업캐스팅(Upcasting)과 다운캐스팅(Downcasting)**의 개념을 이해하고, instanceof를 활용하여 안전하게 타입을 확인하는 방법을 익혔다.
- **메소드 오버라이딩(Overriding)**을 통해 부모 메소드를 자식 클래스에서 재정의할 수 있음을 이해하였다.
- **동적 바인딩(Dynamic Binding)**을 통해 실행 시 실제 객체 타입에 따라 메소드가 결정된다는 것을 확인하였다.
추상 클래스와 인터페이스의 차이점을 이해하고, 인터페이스가 메소드 규격(틀) 역할을 한다는 개념을 학습하였다.
- 객체지향 프로그래밍에서 상속, 다형성, 캡슐화가 어떻게 연결되는지 구조적으로 이해하기 시작하였다.

