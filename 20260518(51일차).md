# 📅 2026-05-18 - JavaScript

## var 변수

자바스크립트 2일차에서 **`var` 변수**는 꼭 이해하고 넘어가야 하는 주제입니다.  
특히 실무에서는 `var`를 새로 쓰는 경우가 거의 없기 때문에,  
**왜 안 쓰는지**, **기존 코드에서 보면 어떻게 해석해야 하는지** 중심으로 정리하는 것이 중요합니다.

---

## 1. `var`란?

`var`는 자바스크립트의 전통적인 변수 선언 방식입니다.

```js
var name = "Kim";
```

예전 자바스크립트 코드에서는 대부분 `var`를 사용했지만,  
현재 실무에서는 보통 `const`와 `let`을 사용합니다.

즉, `var`는 **실무에서 새로 작성하기 위한 문법이라기보다, 레거시 코드를 읽기 위해 반드시 알아야 하는 문법**이라고 보면 됩니다.

---

## 2. 실무에서 `var`를 잘 안 쓰는 이유

### 2-1. 블록 스코프가 없다

`var`는 `{}` 기준으로 범위가 나뉘지 않습니다.  
즉, `if`, `for` 같은 블록 안에서 선언해도 바깥에서 접근할 수 있습니다.

```js
if (true) {
  var message = "hello";
}

console.log(message); // "hello"
```

처음 보면 이상하게 느껴질 수 있습니다.  
보통은 `if` 안에서 만든 변수는 `if` 안에서만 살아있을 것 같지만,  
`var`는 그렇지 않습니다.

실무에서는 이런 동작이 **예상하지 못한 변수 누수**로 이어질 수 있습니다.

---

### 2-2. 함수 스코프를 가진다

`var`는 블록 스코프가 아니라 **함수 스코프(function scope)** 를 가집니다.

```js
function test() {
  var x = 10;
  console.log(x); // 10
}

console.log(x); // ReferenceError
```

함수 내부에서 선언한 `var`는 함수 밖에서는 쓸 수 없지만,  
함수 안의 `if`, `for` 블록에서는 구분되지 않습니다.

```js
function example() {
  if (true) {
    var count = 3;
  }

  console.log(count); // 3
}
```

즉, `var`는 **함수 단위로만 범위가 나뉜다**고 기억하면 됩니다.

---

### 2-3. 중복 선언이 가능하다

`var`는 같은 이름으로 여러 번 선언해도 에러가 나지 않습니다.

```js
var user = "Kim";
var user = "Lee";

console.log(user); // "Lee"
```

이건 실무에서 꽤 위험합니다.  
코드가 길어질수록, 혹은 여러 명이 함께 작업할수록  
같은 이름의 변수를 다시 선언해버리는 실수가 생길 수 있기 때문입니다.

`let`, `const`는 이런 실수를 바로 에러로 잡아주지만,  
`var`는 조용히 덮어씁니다.

---

### 2-4. 호이스팅 때문에 디버깅이 어려워진다

`var`는 선언이 끌어올려진 것처럼 동작합니다.

```js
console.log(age); // undefined
var age = 20;
```

처음 보면 에러가 날 것 같지만 실제로는 `undefined`가 출력됩니다.

자바스크립트 엔진은 내부적으로 대략 아래처럼 처리합니다.

```js
var age;
console.log(age); // undefined
age = 20;
```

이런 특성 때문에 **변수를 잘못된 위치에서 사용해도 즉시 에러가 나지 않아 버그를 놓치기 쉽습니다.**

---

## 3. `var`의 핵심 특징 요약

### 함수 스코프
```js
function test() {
  var x = 10;
}
console.log(x); // 에러
```

### 블록 스코프 없음
```js
if (true) {
  var y = 20;
}
console.log(y); // 20
```

### 중복 선언 가능
```js
var a = 1;
var a = 2;
```

### 선언 전에 접근 가능
```js
console.log(b); // undefined
var b = 3;
```

---

## 4. 실무에서는 무엇을 쓰는가?

실무에서는 보통 아래 원칙을 따릅니다.

### `const` 우선
변수 재할당이 필요 없으면 `const`

```js
const apiUrl = "/users";
```

### 재할당이 필요할 때만 `let`
```js
let page = 1;
page = 2;
```

### `var`는 지양
새 코드에서는 거의 사용하지 않습니다.

즉, 실무 기준으로는:

- 기본은 `const`
- 바뀌는 값만 `let`
- `var`는 레거시 코드 해석용

이라고 생각하면 됩니다.

---

## 5. `var`, `let`, `const` 비교

| 구분 | var | let | const |
|---|---|---|---|
| 스코프 | 함수 스코프 | 블록 스코프 | 블록 스코프 |
| 중복 선언 | 가능 | 불가 | 불가 |
| 재할당 | 가능 | 가능 | 불가 |
| 선언 전 접근 | 가능 (`undefined`) | 불가 | 불가 |
| 실무 사용 | 거의 안 씀 | 자주 사용 | 가장 많이 사용 |

---

## 6. 실무에서 자주 터지는 `var` 문제

### 6-1. 반복문 + 비동기 처리 문제

```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => {
    console.log(i);
  }, 100);
}
```

실행 결과:

```js
3
3
3
```

왜냐하면 `var`는 블록 스코프가 없어서  
각 콜백이 서로 다른 `i`를 가지는 것이 아니라  
**하나의 같은 `i`를 공유**하기 때문입니다.

반복문이 끝난 뒤 `i`는 이미 `3`이 되어 있고,  
`setTimeout` 콜백들이 나중에 실행되면서 모두 `3`을 출력합니다.

### 해결: `let` 사용

```js
for (let i = 0; i < 3; i++) {
  setTimeout(() => {
    console.log(i);
  }, 100);
}
```

실행 결과

```js
0
1
2
```

이 차이는 실무에서 매우 중요합니다.  
이벤트 처리, 비동기 작업, API 호출, 반복 렌더링에서 자주 연결됩니다.

---

### 6-2. 조건문 안 변수 누수

```js
if (isAdmin) {
  var menu = "관리자 메뉴";
}

renderMenu(menu);
```

작성자는 `if` 안에서만 쓴다고 생각했을 수 있지만  
실제로는 바깥에서도 `menu`를 사용할 수 있습니다.

규모가 큰 코드에서는 이런 변수 누수 때문에  
어디서 선언되었는지 추적하기 어려워집니다.

---

### 6-3. 같은 이름 재선언으로 인한 덮어쓰기

```js
var config = getDefaultConfig();

// 중간에 다른 코드 추가
var config = getUserConfig();
```

에러가 나지 않아서 문제를 늦게 발견할 수 있습니다.  
실무에서는 이런 문제를 방지하기 위해 `let`, `const`를 사용합니다.

---

## 7. 실무적인 변수 선언 규칙

### 규칙 1. 기본은 `const`
```js
const userName = "Kim";
const isLoggedIn = true;
const maxRetryCount = 5;
```

### 규칙 2. 값이 바뀌어야 할 때만 `let`
```js
let currentPage = 1;
currentPage += 1;
```

### 규칙 3. `var`는 새 코드에서 사용하지 않기
```js
// 지양
var total = 0;

// 권장
let total = 0;
```

### 규칙 4. 변수의 범위는 최대한 좁게 가져가기
필요한 위치 가까이에서 선언하는 것이 좋습니다.

```js
function printUser(user) {
  const name = user.name;
  console.log(name);
}
```

---

## 8. 좋은 변수명 습관

실무에서는 변수명도 중요합니다.  
특히 `var`를 쓸 때는 범위와 역할이 모호해지기 쉬워서  
더욱 의미 있는 이름을 사용하는 습관이 중요합니다.

### 나쁜 예
```js
var a = 10;
var data = {};
var temp = "Kim";
```

### 좋은 예
```js
const maxRetryCount = 10;
const userProfile = {};
const userName = "Kim";
```

좋은 변수명은 코드를 읽는 시간을 줄여줍니다.

---

## 9. 자주 하는 오해

### `const`면 객체 내부도 수정 불가능한가?

아닙니다. `const`는 **변수 재할당만 막습니다.**

```js
const user = { name: "Kim" };
user.name = "Lee"; // 가능
```

하지만 변수 자체를 다른 값으로 바꾸는 것은 불가능합니다.

```js
user = {}; // 불가
```

즉:

- 객체 내부 값 변경: 가능
- 변수 자체 재할당: 불가

입니다.

---

## 10. 면접/실무에서 자주 묻는 포인트

### Q1. `var`와 `let`의 가장 큰 차이는?
- `var`: 함수 스코프
- `let`: 블록 스코프

### Q2. 왜 `var`를 실무에서 피하나요?
- 블록 스코프가 없음
- 중복 선언 가능
- 호이스팅으로 버그 찾기 어려움

### Q3. 그럼 `var`는 왜 배워야 하나요?
- 오래된 코드에서 자주 등장
- 레거시 프로젝트 유지보수 시 필요
- 기존 문법을 이해해야 리팩토링 가능

---

## 11. 오늘의 핵심 한 줄 정리

> `var`는 오래된 변수 선언 방식이고, 실무에서는 `const`를 기본으로 쓰고 필요할 때만 `let`을 사용한다.

---

## 12. 실무 스타일 예제

### 좋지 않은 예
```js
var user = "Kim";
var count = 0;

if (true) {
  var message = "hello";
}

for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 100);
}
```

### 더 좋은 예
```js
const user = "Kim";
let count = 0;

if (true) {
  const message = "hello";
  console.log(message);
}

for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 100);
}
```

---

## 13. 복습 체크포인트

오늘 `var`를 공부했다면 아래 4개는 꼭 기억하면 됩니다.

- `var`는 **함수 스코프**
- `var`는 **중복 선언 가능**
- `var`는 **호이스팅 시 `undefined`**
- 실무에서는 **`const` / `let` 사용**

---

## 14. 직접 실행해보면 좋은 예제

### 예제 1. 호이스팅
```js
console.log(a);
var a = 1;
```

### 예제 2. 블록 스코프 없음
```js
if (true) {
  var x = 10;
}
console.log(x);
```

### 예제 3. 반복문 + 비동기
```js
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

이후 전부 `let` 또는 `const`로 바꿔서 차이를 비교해보면 훨씬 잘 이해됩니다.

---

## 15. 발표/노트용 짧은 요약

### `var`
- 옛날 변수 선언 방식
- 함수 스코프
- 중복 선언 가능
- 호이스팅 시 `undefined`
- 실무에서는 거의 사용하지 않음

### 실무 원칙
- 기본은 `const`
- 바뀌는 값만 `let`
- `var`는 레거시 코드에서 이해용으로 학습

---
