# 59일차 학습 요약: C++, 컴퓨터 비전 및 Linux 네트워크

## 1. C++ 핵심 문법 및 객체 지향 프로그래밍 (OOP)

C++은 메모리를 직접 제어하면서도 객체 지향 패러다임을 통해 대규모 시스템을 구조화할 수 있는 강력한 고급 언어입니다. 58일차에는 데이터의 안전성을 높이는 클래스 설계와 다차원 데이터 처리를 학습했습니다.

### ① Fraction(분수) 클래스 설계
분수 구조는 정수 형태의 분자(`numerator`)와 분모(`denominator`)를 하나로 묶어 관리하는 대표적인 사용자 정의 식별자(객체) 예시입니다. 
* **데이터 멤버 (`private`)**: 분자와 분모는 외부에서 임의로 수정할 수 없도록 사생활 보호(`private`) 영역에 둡니다. 분모가 0이 되는 논리 오류를 원천 차단하기 위함입니다.
* **멤버 함수 (`public`)**: 외부와 소통하는 인터페이스를 구현합니다.
  * **기약분수 변환 (Reduce)**: 분자와 분모의 최대공약수(GCD)를 구해 연산 결과를 항상 가장 단순한 형태로 유지합니다.
  * **사칙연산 오버로딩**: 두 분수 객체를 더할 때 공통 분모를 만드는 통분 알고리즘을 함수 내부에 구현합니다.

### ② Time(시간) 클래스 설계
시간 데이터를 시, 분, 초 단위로 쪼개어 다루는 객체입니다.
* **무결성 검증 (Validation)**: 초/분이 60 이상이 되거나 시가 24 이상이 될 때 자동으로 상위 단위로 받아올림(Carry) 처리를 해주는 로직이 핵심입니다.
* **접근 연산자 (`.`)와 캡슐화**: 외부에서는 `timeObj.hour = 25;`와 같이 직접 값을 대입할 수 없으며, 멤버 함수인 `timeObj.setTime(23, 59, 59);`를 통해 안전하게 접근해야 합니다.

### ③ Array Attributes (배열 속성) 및 2차원 배열
* **메모리 연속성**: C++에서 2차원 배열은 가로, 세로의 개념을 가지지만, 실제 메인 메모리(RAM) 상에는 1차원 형태로 연속되게 배치됩니다(행 우선 순서).
* **중첩 루프 접근**: 2차원 그리드 데이터를 전체 탐색할 때는 외각 루프가 행(Row)을, 내부 루프가 열(Column)을 담당하도록 제어합니다.
  ```cpp
  int matrix[3][4]; // 3행 4열의 2차원 배열 선언
  for (int i = 0; i < 3; i++) {
      for (int j = 0; j < 4; j++) {
          matrix[i][j] = i + j; // 인덱스를 활용한 순차 접근
      }
  }

  ## 2. 컴퓨터 비전 (OpenCV 기반 결함 탐지)

자동차 부품 제조 등 산업 현장(비전 검사)에서 부품의 크랙, 스크래치, 기포 등의 불량을 자동으로 탐지하고 분류할 때 사용하는 OpenCV의 핵심 디지털 영상 처리 공정입니다.



### ① 그레이스케일 변환 (`cv::cvtColor`)
* **목적**: 컬러 영상(RGB 3채널)을 밝기 정보만 가진 흑백 영상(1채널)으로 변환합니다.
* **이유**: 산업용 카메라는 초당 수십~수백 장의 프레임을 처리해야 하므로, 데이터 양을 1/3로 줄여 하드웨어(CPU/GPU) 연산 속도를 극대화하기 위함입니다.

### ② 이진화 처리 (`cv::threshold`)
* **목적**: 특정 임계값(Threshold)을 기준으로 영상의 픽셀을 완전히 검은색(0)과 흰색(255) 두 가지 값으로만 분리합니다.
* **이유**: 부품의 정상적인 표면(배경)과 어둡거나 밝게 도드라지는 스크래치(결함 영역)를 명확하게 이분법적으로 분리하여 컴퓨터가 인식할 수 있는 객체 상태로 만들기 위함입니다.

### ③ 형태학적 필터링 (`cv::morphologyEx`)
이진화 과정을 거치면 조명 변화나 미세한 먼지 때문에 노이즈(외딴 점)가 생기거나, 하나의 결함이 여러 개로 쪼개져 보이는 부작용이 생길 수 있습니다. 이를 보정하기 위해 **모폴로지(Morphology) 연산**을 적용합니다.
* **침식(Erosion)**: 배경에 있는 작은 노이즈 점들을 갉아먹어 제거합니다.
* **팽창(Dilation)**: 결함 영역의 경계를 확장하여 내부의 미세한 구멍을 메웁니다.
* **열림/닫힘 연산**: 침식 후 팽창(열림)을 하면 미세한 돌기나 노이즈가 완벽히 제거되며, 팽창 후 침식(닫힘)을 하면 끊어진 결함의 윤곽선 라인이 하나로 매끄럽게 연결됩니다.

### ④ 윤곽선 추출 (`cv::findContours`)
* **목적**: 이진화 및 모폴로지 필터링이 끝난 흰색 객체들의 외곽 경계선 좌표를 모두 추출해 냅니다.
* **후처리 분류**: 추출된 외곽선 정보를 기반으로 객체의 면적(`contourArea`)이나 길이, 종횡비를 계산합니다. 이를 통해 설정한 기준치보다 면적이 넓거나 형태가 찌그러진 개체를 '불량 부품'으로 판정하고 최종 분류(Classification) 단계를 수행합니다.

---

## 3. 리눅스 핵심 네트워크 명령어

리눅스 서버 및 네트워크 환경을 모니터링하고 연결 상태의 이상 유무를 진단할 때 사용하는 필수 명령어 도구 모음입니다.

| 명령어 | 주요 기능 및 옵션 설명 | 실제 활용 시나리오 |
| :--- | :--- | :--- |
| **`ifconfig`**<br>**`ip addr`** | 시스템의 네트워크 인터페이스(LAN 카드, Wi-Fi 등) 활성화 상태와 할당된 IP 주소, 서브넷 마스크, MAC 주소를 확인합니다. | 서버를 네트워크에 처음 연결한 후 내부 IP 주소가 제대로 할당되었는지 확인할 때 사용합니다. |
| **`ping`** | ICMP Echo Request 패킷을 목적지 서버로 전송하고 응답이 돌아오는 시간(ms)과 패킷 손실률을 측정합니다. | 외부 인터넷 망이나 특정 데이터베이스(DB) 서버와 물리적으로 통신이 끊김 없이 유지되는지 확인할 때 사용합니다. |
| **`netstat`**<br>**`ss`** | 시스템의 라우팅 테이블, 소켓 연결 상태를 확인합니다.<br>• `-a`: 모든 소켓(연결 완료 및 대기 중) 출력<br>• `-n`: 도메인 네임 대신 숫자로 표현(속도 빠름)<br>• `-t`/`-u`: TCP / UDP 프로토콜 필터링 | 웹 서버가 80번 또는 443번 포트로 정상적인 대기(`LISTEN`) 상태를 유지하고 있는지, 외부 클라이언트가 몇 명이나 접속(`ESTABLISHED`)해 있는지 모니터링할 때 사용합니다. |
| **`nslookup`** | DNS(Domain Name System) 서버에 직접 질의하여 특정 도메인 주소의 IP 주소를 변환해 오거나 반대의 경우를 조회합니다. | "인터넷 주소창에 도메인을 치면 접속이 안 되는데, IP 주소를 직접 입력하면 접속이 될 때" DNS 서버 설정 오류를 찾아내는 용도입니다. |
| **`curl`**<br>**`wget`** | 터미널 환경에서 백그라운드로 파일을 다운로드하거나 웹 페이지 요청을 주고받는 도구입니다.<br>• `curl -I`: HTTP 헤더 정보만 수신<br>• `wget [URL]`: 원격 파일 다운로드 | 웹 브라우저(GUI)가 없는 리눅스 환경에서 외부 오픈소스 라이브러리나 패키지 파일을 다운로드받을 때, 혹은 개발한 REST API 서버의 응답 코드(200 OK 등)가 정상 작동하는지 테스트할 때 사용합니다. |